# FinSynagent 模块讲解（最新版）

> 本文档基于 `fin_synagent/app.py` 当前代码状态整理（对应 commit `3de9b06` 之后的线上版本），覆盖双模式（标准版 / 简略版）、三大核心模块（Consult / Screen / RAG）及支撑页面。所有关键数字均为真实评测/建库值，非演示编造。

---

## 0. 项目一句话定位

FinSynagent 是一个**多智能体协同的智能投顾系统**：用户输入投资问题或选定行业偏好后，系统通过多智能体工作流（Consult）组织回答、通过筛选树（Screen）给出个股推荐，并全程由 RAG 知识库提供**可溯源**的权威依据。系统以 **Streamlit 多页应用**交付，提供「标准版 / 简略版」两种入口，覆盖投资者、技术评审、面试准备三类受众。

**技术栈要点**：Python + Streamlit；向量库 Chroma；中文嵌入 BAAI/bge-small-zh-v1.5（512 维）；多智能体编排基于 LangGraph 状态图；基座大模型为金融微调版星火大模型（本地以 bge 等价替代 Embedding，下游可平滑切换到星火 API）。

---

## 1. 整体架构与双模式

### 1.1 单文件 + 派生导航

整个应用是**单文件 `app.py`**（约 4200 行），UI 全部在模块顶层，页面由 `NAV_PAGES` 注册、`PAGES` 字典映射到 `page_*` 函数。简略版不做独立代码分支维护，而是从标准版**派生**：

```python
EXCLUDE_IN_LITE = ["首页", "星火大模型", "技术设计", "技能中心", "测试评估"]
NAV_PAGES_LITE = [p for p in NAV_PAGES if p not in EXCLUDE_IN_LITE]
```

新增页面只需改 `NAV_PAGES` / `NAV_ICONS` 一处，简略版自动同步，避免「两版数字/文案漂移」。

### 1.2 页面清单（标准版 11 页）

| # | 页面 | 说明 |
|---|------|------|
| 1 | 首页 | 能力详解（Consult 六智能体 + Screen 三层筛选树）+ 版本迭代 |
| 2 | 痛点 | 传统投顾/投研的痛点与本项目解法 |
| 3 | 智能咨询 | Consult 多智能体工作流（标准版可真实运行） |
| 4 | 智能荐股 | Screen 筛选树（标准版可真实运行） |
| 5 | 星火大模型 | 基座模型与微调方案 |
| 6 | 测试评估 | 生成评测与三层评测体系 |
| 7 | RAG 知识库 | 真实建库规模 + 检索样本浏览器 + RAG 指标 |
| 8 | 技能中心 | 能力/技能清单 |
| 9 | 技术设计 | 架构与工程细节 |
| 10 | 专有名词解释 | 核心术语卡（29 条，含大白话） |
| 11 | 面试建议 | 简历 + STAR 行为题 + 技术高频问答 |

### 1.3 简略版（6 页）

`痛点 / 智能咨询 / 智能荐股 / RAG 知识库 / 专有名词解释 / 面试建议`

简略版**不运行流程**，只做「简易讲解 + 大白话批注」，并把技术高频问答收敛为 RAG / 通用两组（去掉微调与训练、LangGraph 两组），名词解释仅展示标星（⭐）或 `CORE_TERMS` 白名单内的核心术语。

---

## 2. 三大核心模块

### 2.1 Consult 智能咨询 —— 多智能体工作流（LangGraph）

把用户的一个投资问题，交给一队 AI 角色开「投资研讨会」：有人拆解、有人翻库、有人写稿、有人挑刺、有人核查、有人总结，全过程透明可观测、结论带出处。

**标准版 7 节点状态图（`_run_workflow_inline`）**：

1. **用户输入** —— 问题原样保留为共享上下文，不提前改写。
2. **Leader Agent（拆解 / 分配）** —— 按宏观面 / 估值面 / 基本面 / 风险面拆为子任务，生成结构化提示词分派给行业 Expert，支持人机协同补充。
3. **RAG 检索** —— bge 编码问题向量，在 Chroma 对应行业 collection 余弦 Top-K 召回，取出带 PDF 来源与页码的 chunk 注入 Expert。
4. **Expert Agent（作答）** —— 白酒 / 红利 / 贵金属 / 宏观专家基于子任务 + RAG 资料写结构化分析，每条结论标注 `[XXX.pdf pNN]`。
5. **Critic Agent（批评）** —— 自我对抗审查数值口径、逻辑一致性、结论具体性、风险遗漏；并用 qstock 拉实时行情财报交叉验证。
6. **Verify Agent（校验）** —— 把最终答案与「知识库原文 + 联网数据」逐条比对，压低幻觉。
7. **Summary Agent（总结）** —— 给出可执行的结论并邀请追问。

> 简略版用 `_render_consult_lite()` 以「📘 专业讲解 + 🗣 大白话」双栏呈现这七步，不实际调用模型。

### 2.2 Screen 智能荐股 —— 筛选树（三层结构）

像一个基金经理团队：先抓数据 → 四维体检 → 汇总打分挑状元。

**标准版 12 节点筛选树**，对外统一归纳为**三层**：

- **① 用户输入**：选定目标行业（白酒 / 红利 / 贵金属 / 宏观）与风险偏好（保守 / 平衡 / 进取），编码为筛选约束。
- **② 数据获取层（4 子项）**：实时行情获取 → 意图解析（行业+偏好映射为结构化条件）→ 股票池构建（行业过滤 + 市值>500 亿、非 ST 门槛）→ 个股评论抓取（股吧 / 论坛，为情绪面备料）。
- **③ 四维特征层（4 子项）**：基本面（营收/净利/ROE/估值分位）→ 技术面（均线/MACD/量价）→ 情绪面（FinBERT 情感极性与热度）→ 行业面（景气度/政策/产业链位置）。
- **④ 汇聚决策层（4 子项）**：四维特征汇聚成个股画像 → LLM 资深分析师视角 0–100 打分（茅台 91.2 / 五粮液 86.7 …）→ 分析师观点与推荐理由 → 校验与反思 + 风险提示声明。

> 简略版用 `_render_screen_lite()` 同样以三层 + 子项 + 大白话讲解，不运行筛选树。首页「能力二」以精简卡片（标题 + 一句摘要 + 子项标签）概览同一结构。

### 2.3 RAG 知识库 —— 可溯源的私人图书馆

解决大模型「记性有限、爱瞎编」：把权威 PDF 拆块、向量化入库，提问时召回最像的原文片段喂给大模型「看着资料答」。

**建库流程（6 步）**：收资料（4 行业 PDF）→ 语义切分 chunk（300–800 字）→ bge 转 512 维向量 → 存入 Chroma（按行业分 4 个 collection）→ 问题同样转向量 → 余弦 Top-K 召回 + 大模型作答。

**真实建库规模**：152 份 PDF / 12,594 chunk，分行业：

| 行业 | PDF 份数 | chunk 数 |
|------|---------|----------|
| 白酒 | 47 | 2,973 |
| 红利 | 36 | 4,972 |
| 贵金属 | 36 | 3,601 |
| 宏观 | 33 | 1,048 |

**真值评测指标**（2,754 条 chunk-derived 查询；LLM-as-Judge `mode=llm`）：

- 检索：Recall@5 ≈ **0.9628**｜Precision@5 ≈ 0.8881｜MRR ≈ 0.9385｜NDCG@5 ≈ 0.9348｜来源覆盖 ≈ 2.52
- 生成：Faithfulness（忠实度）≈ **0.8988**｜Answer Relevance ≈ 0.8666｜Context Utilization ≈ 0.8744
- 分行业 Recall@5：白酒 0.9921｜红利 0.9941｜贵金属 0.9987｜**宏观 0.8663（唯一明显短板）**

> 评测口径与 BEIR / MS MARCO / RAGAS 同源：检索质量看「该找的找没找到、排得顺不顺」（Recall/MRR/NDCG），生成质量看「有没有瞎编、答没答到点、用没用上资料」（忠实度/相关性/利用率）。页面提供检索样本浏览器（真实召回片段 + 余弦相似度 + 来源页码）与指标明细。

---

## 3. 支撑模块

| 模块 | 归属 | 要点 |
|------|------|------|
| 首页 | 双版（标准有，简略无） | 能力详解：Consult 六智能体卡片 + Screen 三层筛选树概览 + 版本迭代时间线 |
| 痛点 | 双版 | 传统投研/投顾痛点 → 本项目解法，承上启下 |
| 星火大模型 | 仅标准版 | 基座模型能力、金融微调（SFT：强化语义捕捉 + 统一专业口吻 + 内建合规约束「不构成投资建议」） |
| 测试评估 | 仅标准版 | 生成评测（AI as Judge + AI as Customers + 人工交叉）+ 显著性检验 |
| 技能中心 | 仅标准版 | 能力/技能清单 |
| 技术设计 | 仅标准版 | 架构、工程细节、两段式检索（Top-100 召回 → 重排 Top-5） |
| 专有名词解释 | 双版 | `glossary.json` 驱动，核心术语卡（标星或 `CORE_TERMS` 白名单）；简略版附「🗣 大白话」 |
| 面试建议 | 双版 | 简历项目介绍 + STAR 行为题 + 技术高频问答；简略版仅 RAG / 通用两组标星题，附大白话 |

**名词可见性机制**：`_is_core(t)` = `t.star == True` 或 `t.term in CORE_TERMS`；`CORE_TERMS` 白名单（`app.py` 约 4042 行）含 多智能体 / RAG / 知识库 / Embedding / 检索 / Chunk / Chroma / BGE / 余弦相似度 / Top-K / 微调 / 情感分析 / 大模型 / 智能体 / 提示词 / 幻觉 / AI as Judge 等。

---

## 4. 简略版 ↔ 标准版 对应速查

| 能力 | 标准版 | 简略版 |
|------|--------|--------|
| 智能咨询 | 7 节点 LangGraph 真实运行 | 七步「专业+大白话」讲解，不运行 |
| 智能荐股 | 12 节点筛选树真实运行 | 三层 + 子项「大白话」讲解，不运行 |
| RAG 知识库 | 规模 + 样本浏览器 + 指标 | 大白话讲解 + 规模/KPI |
| 专有名词 | 全部术语 | 仅核心术语（标星 / 白名单）+ 大白话 |
| 面试建议 | 全部分组 + 全部题 | RAG / 通用两组标星题 + 大白话 |
| 首页 / 星火 / 技术设计 / 技能中心 / 测试评估 | 有 | 无 |

---

## 5. 关键数字速查

- 语料：152 PDF / 12,594 chunk（白酒 2,973｜红利 4,972｜贵金属 3,601｜宏观 1,048）
- 检索 Recall@5 ≈ 0.963；来源覆盖 ≈ 2.52
- 生成 Faithfulness ≈ 0.90；Answer Relevance ≈ 0.867；Context Util ≈ 0.874
- 宏观行业 Recall@5 最低 ≈ 0.866（当前唯一重点优化方向）
- 嵌入：BAAI/bge-small-zh-v1.5，512 维，L2 归一化（余弦 = 点积）
- 检索链路：余弦 Top-100 召回 → 重排 → Top-5 生成依据

---

*文档随代码同步更新；若页面结构（NAV 排除名单、术语白名单、指标真值）发生变化，以 `app.py` 当前实现与评测输出为准。*
