# 面试手册 · RAG 方向（以 Fin Synagent 为主要项目）

> **适用场景**：面试岗位是 RAG / 检索 / 知识库 / 大模型应用方向，你打算以 Fin Synagent 作为主项目讲。
> **核心策略**：不要说"我做过一个多智能体投顾项目"，而要**重述为"我负责了一条完整的金融 RAG 链路"**——把 RAG 提到最前面，多智能体只当背景。
>
> **配套文档**：数字与结论取自《FinSynagent_三大模块环节详解.md》（技术版）与《零基础通俗讲解.md》，三份文档口径完全一致。

---

# 第 0 节 · 先想清楚面试官在期待什么

当你说"我主要工作是 RAG"，面试官脑子里会立刻挂上三个待验证项。你的整个表达都应该服务于这三个：

| 面试官想验证的 | 他会问什么 | 你要拿出的证据 |
|---|---|---|
| **① 你懂不懂整条链路** | "你的 chunk 怎么切的？为什么这么切？" | 五个环节一步不落，每一步都能说出**参数和理由** |
| **② 你能不能说清效果** | "你的召回率多少？怎么测的？" | 一个**不可作弊的评测方法** + 一组真实数字 |
| **③ 你有没有踩过坑、会不会做取舍** | "有什么问题没解决？为什么？" | **诚实的短板 + 归因 + 下一步方向** |

**最重要的一句话**：RAG 面试的分水岭不是"你答对了多少"，而是**"你敢不敢承认哪里不行，并且能说清为什么不行"**。你的项目恰好在这个维度上特别强——那 7.6% 就是你的王牌，别藏着。

---

# 第 1 节 · 项目重述：把项目改写成「RAG 视角」

## 一句话定位（背下来）

> **"我负责的是一个金融行业的 RAG 知识库——从 152 份公开年报和央行报告出发，建了一条从解析、切分、向量化、入库到检索、重排、评测的完整链路，并且把它接进了一个多智能体的投顾工作流里。我做得最扎实的部分是评测：2,754 条查询的实测指标，以及两次重排实验——第一次没能证明重排有用，第二次证明了我当初选的重排器本来就不是最优选。"**

注意这句话的三个设计：
1. **主语是"RAG 知识库"**，不是"投顾系统"；
2. **"我负责"**三个字明确职责边界（面试官最反感"我们团队做了……"）；
3. **主动埋钩子**——"评测"和"两次重排实验"是最能吸引追问的两个点，等于把面试官往你最强的地方引。

## 职责边界怎么说（被问"你具体做哪部分"）

> "整条链路我都是主实现：离线侧是语料归集、PDF 解析、语义切分、向量化、入库；在线侧是行业路由、查询编码、检索、重排、Prompt 约束；另外我把检索封成一个独立模块 `kb.py`，让多智能体工作流里的检索节点直接调用。评测脚本也是我写的——这是花时间最多的部分。"

**关键**：把"检索封装成独立模块"这个动作说出来。这体现了**模块边界意识**，是 RAG 岗很看重的工程素养。

---

# 第 2 节 · 分档自我介绍脚本（全部以 RAG 为主语）

## 30 秒版（前台/收尾/边角时间）

> "我主要做 RAG。最近一个项目是金融行业的 RAG 知识库：152 份公开年报和政策报告，切成 12,594 个片段，用 bge 编码后按行业分 4 个向量库。
> 我做得最深的是**评测**——2,754 条查询实测，实体级召回 Recall@5 是 96.3%，但精确命中源片段只有 7.6%。我把这个 7.6% 拆开做了归因，发现不是检索坏了，而是同公司片段高度同质、互相挤占。于是我做了两轮重排实验想解决它：第一轮没能证明重排有用，但逼我算出了**真正的瓶颈是检索覆盖率**——63% 的标准片段压根不在候选池里；第二轮我回去验证第一轮选的方案对不对，结果是**我当初选它只是因为模型没下载全**，换成真 cross-encoder 后小样本对照里精确命中率是三路里最高的。两次实验一次给了我瓶颈、一次修正了我自己的选型。"

## 1 分钟版（最常用，主流程）

> "我主要做 RAG，主要是金融垂直领域的知识库。
>
> **链路**：离线侧，我把 152 份公开 PDF（公司年报、央行货币政策报告）用 PyMuPDF 两遍扫描做结构化提取——第一遍统计正文字号中位数，第二遍用'字号大于正文 1.22 倍且行很短'来识别标题层级；然后基于标题层级做语义切分，单块上限 520 字，并用中文占比 45% 这条线过滤掉双语年报里的英文噪声；最后用 bge-small-zh 编码、L2 归一化，按行业分 4 个 Chroma collection 入库，一共 12,594 个片段。在线侧是行业路由 → 查询编码（带 bge 的指令前缀）→ 余弦召回 Top-100 → 混合重排取 Top-5 → 约束式 Prompt 生成并要求标注来源。
>
> **评测**：我没用人工手写的问题集，因为那样指标容易虚高。我用的是业界标准的 chunk-derived 方法——从真实片段反向合成问句，片段所属公司就是标准答案，与 BEIR / RAGAS 同源，2,754 条查询覆盖四个行业。
>
> **结果和短板**：实体级 Recall@5 是 96.3%，NDCG@5 是 0.94，检索本身是强的；但'精确命中源头那一段'只有 7.6%。我做了归因：未命中的查询里有 86.6% 的 Top-5 至少含一个同公司片段，说明是实体内的片段互挤，不是跨实体找错。
>
> **重排我做了两轮实验，结论都不是我一开始预期的那种**。第一轮上 late-interaction 重排，全局只涨 0.32 个点，基本算失败——但它帮我算清了真正的问题：有 63% 的标准片段根本不在 Top-100 候选里，所以任何'只重排已召回候选'的方法在结构上就够不到它们，**瓶颈是检索覆盖率而不是排序**。
>
> 第二轮我回头质疑了自己第一轮的选型：当初不用 cross-encoder 是因为模型下载不下来，不是因为 late-interaction 更好。于是我把那个模型找出来做成 ONNX 推理，做了三路受控对照——纯向量基线、late-interaction、真 cross-encoder。100 条查询的结果是 6% / 10% / 17%，**四个行业上顺序完全一致，真 cross-encoder 全面胜出**。样本还小、绝对幅度要全量重跑才能确认，但它至少说明了一件事：我当时那个'够用就行'的判断是错的，而我自己把它查出来了。"

## 3 分钟版（面试官说"详细讲讲"）

在 1 分钟版的基础上按这个顺序展开，每段 30–45 秒：

1. **背景与目标**（20 秒）：金融领域 RAG 的核心痛点是幻觉和不可溯源 → 我的目标是"答案必须能标到 PDF 的页码"。
2. **离线建库**（45 秒）：五个步骤 + 每步的关键参数与理由（字号中位数判标题、520 字上限、45% 中文占比、512 维 L2 归一化、四库隔离的理由）。
3. **在线查询**（45 秒）：五个环节 + 两个细节（bge 指令前缀为什么必须加、为什么召回 100 再精排到 5）。
4. **评测设计**（40 秒）：为什么弃用人工问题集、chunk-derived 怎么保证不可作弊、相关性怎么判定（实体级 + 分级 NDCG）。
5. **结果与归因**（30 秒）：96.3% / 7.6% / 归因结论。
6. **两次重排实验与结论**（45 秒）：第一轮为什么"失败"、覆盖率天花板怎么算出来的；第二轮怎么发现自己选型有问题、三路对照怎么做、结论是什么。

**收尾一句话**（很重要，别直接结束）：
> "这个项目对我最大的价值不是指标多高，而是**我知道了 RAG 的指标该怎么设计才不可作弊，以及一个检索系统到底该怎么定位瓶颈**。"

---

# 第 3 节 · 王牌：一条完整的主线故事（STAR）

面试官一定会说"讲一个你解决过的难题"。**就用这条，别用别的。** 它同时展示了评测能力、归因能力、实验设计能力和诚实。

### S · 情境

> "最早我的检索评测是**人工手写 152 条问题**，然后用实体字符串匹配当相关性判定。跑出来指标很好看——但我不太信：问题是我自己写的、答案也是我自己定的标准，这等于自己出题自己判卷。"

### T · 任务

> "我要把评测改成**不可作弊**的，让它能真实反映系统水平——哪怕结论是难看的。"

### A · 行动（四步，重点讲第三、四步）

**第一步：重做评测口径**

> "我换成业界标准的 chunk-derived 方法：从知识库真实的 12,594 个片段里，筛出合格片段——散文型、长度 60 到 800 字、含数字、命中金融关键词表，筛完是 8,115 个——然后**为每个片段反向合成一个'答案就在这一段里'的问句**，这个片段所属的公司或政策主题就是标准答案。这样查询是由语料自身生成的，我没法挑简单题，和 BEIR / RAGAS 的做法同源。"

**第二步：区分"实体级"和"精确片段"两个指标**

> "我把指标拆成两层：**实体级**是'Top-5 里有没有命中正确主体'，**精确片段级**是'Top-5 里有没有正好命中那一段'。这一拆就出问题了——实体级 96.3%，精确级只有 7.6%。我立刻意识到这两个数的落差本身才是最有价值的信号。"

**第三步：归因 + 实验**

> "我做了归因：统计全部 2,754 条查询，Top-5 里平均有 2.95 条来自同一家公司；未命中的查询里 86.6% 至少含一个同公司片段。所以不是'找错公司'，是'同一家公司上百个片段长得太像、互相挤'。
>
> 于是我上重排做实验。第一版是纯 late-interaction（ColBERT 式 MaxSim），结果很有意思：白酒 +3.5 个点、红利 +2.4 个点，说明对'兄弟挤占'确实有效；但宏观 -3.9 个点、贵金属 -1.7 个点，因为概览类查询本来排得好，被重排扰动反而退化了。所以我改成混合：**0.6 倍 bi-encoder 余弦 + 0.4 倍 late-interaction 归一化混合**，全局从 7.27% 到 7.59%。
>
> 顺便这里有个工程优化：原来每个查询都要重新编码 100 个候选，2,754 条查询跑 20 多分钟。我改成**一次性预计算整个 collection 所有片段的 token 级表征**，每个查询只编码自己一次然后做矩阵乘——从 20 多分钟压到 2 分钟左右。"

**第四步：回头检验自己的选型（这步最能体现主动性）**

> "做完全局评测之后我复盘了一件事：我当初为什么不用 cross-encoder？理由写的是'HF 下载太慢，256MB 要 11 分钟'——**这是个环境理由，不是技术判断**。也就是说我可能一直在用一个退而求其次的方案，却把它当成结论写进了文档。
>
> 所以我把那个模型找出来重新处理：缓存里那份其实是 ONNX 格式，sentence-transformers 的 CrossEncoder 加载不了，我绕开它、直接走 onnxruntime 推理。然后设计了**三路受控对照**——同一批查询、同一个 Top-100 候选池、同一套命中判定口径，只换最后一步的排序器：
>
> ① 纯 bi-encoder（不重排，基线）② late-interaction hybrid（我上线的方案）③ 真 cross-encoder（bge-reranker-base）。
>
> 结果是 6% / 10% / **17%**，而且**四个行业上顺序完全一致**——cross-encoder 全面胜出，包括红利和贵金属这两个另两路都拿零蛋的行业。"
>
> ⚠️ **面试时这句一定要主动说**：这个对照每个行业只抽了 25 条、总共 100 条，绝对幅度不能当结论用（10% 这个量级在 n=100 下置信区间有 ±6 个点），要变成主指标得全量重跑 2,754 条。**但方向性是稳的**——四个行业无一例外。主动交代样本量，比让对方追问出来好得多。

### R · 结果与反思（这段是加分核心）

> "第一轮实验的结论看起来是'重排没用'——只涨 0.32 个点，基本算失败。但我没停在这：我统计了标准片段在 bi-encoder 全库里的排名分布——白酒只有 37.9%、红利 32.7%、贵金属 30.4% 落在 Top-100 以内，**大约 63% 的标准片段根本不在候选里**。所以准确的说法不是'重排没用'，而是**重排只能在候选池内部起作用，它的天花板被检索覆盖率锁死了**。
>
> 第二轮实验修正的是我自己的一个判断失误：第一轮我选 late-interaction，理由写的是'零额外下载'——那是环境约束，不是技术结论，我却把它当结论用了。补做三路对照后，真 cross-encoder 在四个行业上全面胜出。
>
> 所以合起来是两个层次不同的结论，我在文档里也是分开写的：**① 候选池内部还有空间，换更强的重排器就能拿到（二阶优化，上限明确）；② 想突破那个上限，必须提高检索覆盖率——换更强或金融领域化的 embedding、做查询改写、或者切得更细。** 第一条我已经量化了差距，第二条是我接下来的主线。"

**为什么这条故事强**：它完整走完了"发现问题 → 改方法 → 数据归因 → 设计实验 → 实验失败 → 从失败里提炼更本质的结论 → **回头检验自己当初的技术选型、发现它是权宜之计、再补一轮对照量化差距** → 修正方向"。**面试里最稀缺的不是成功案例，而是这种"我知道自己为什么没成功、也知道自己当初哪里偷了懒"的案例。**

---

# 第 4 节 · 数字卡（必背，被追问能秒答）

| 项 | 数值 | 备注（被追问时补） |
|---|---|---|
| 语料规模 | **152 份 PDF** | 白酒 47 / 红利 36 / 贵金属 36 / 宏观 33 |
| 片段总数 | **12,594** | 白酒 2973 / 红利 4972 / 贵金属 3601 / 宏观 1048 |
| 合格片段（评测用） | **8,115** | 白酒 1695 / 红利 3172 / 贵金属 2266 / 宏观 982 |
| 向量模型 | bge-small-zh-v1.5 | **512 维**，L2 归一化 |
| 切分参数 | 上限 520 字 / 下限 120 字 / 中文占比 ≥0.45 | — |
| 评测查询数 | **2,754 条** | 白酒 755 / 红利 852 / 贵金属 773 / 宏观 374 |
| Recall@5 | **0.9628** | 实体级 |
| Precision@5 | 0.8881 | — |
| MRR / NDCG@5 | 0.9385 / 0.9348 | — |
| 精确片段命中率@5 | **0.0759** | 重排前 0.0727 |
| 余弦相似度均值（Top-1） | 0.6651 | — |
| Top-5 来源覆盖 | 2.521 | Top-5 平均覆盖 2.52 个不同来源文档 |
| 生成·忠实度 | **0.8988** | LLM-as-Judge，deepseek-chat 实跑 |
| 生成·答案相关性 / 利用率 | 0.8666 / 0.8744 | — |
| 分行业 Recall@5 | 白酒 0.9921 / 红利 0.9941 / 贵金属 **0.9987** / **宏观 0.8663** | 宏观是短板 |
| 分行业精确命中 | 白酒 0.0742 / 红利 0.0587 / 贵金属 0.0582 / **宏观 0.1123** | 宏观反而最高 |
| 重排提速 | 20+ 分钟 → **约 2 分钟** | 预计算 token 表征 |
| 重排增益（上线方案 late-interaction） | 7.27% → **7.59%**（+0.32pt） | 白酒/红利上升；**贵金属小幅回落** 6.08%→5.82% |
| 重排三路对照（小样本 n=100） | 纯向量 **6%** / late-interaction **10%** / **真 cross-encoder 17%** | 四行业排序完全一致；绝对幅度需全量重跑 |
| gold 落在 Top-100 内 | 白酒 37.9% / 红利 32.7% / 贵金属 30.4% / 宏观 58.6% | **约 63% 在外** |

> ⚠️ 背的时候注意四个**容易被记错**的地方：
> ① 精确命中率是 **7.6%**（不是 7.3%，7.3% 是重排前的 baseline）；
> ② 宏观的 Recall 最低（86.6%）但精确命中**最高**（11.2%）——这是一对反直觉数字，恰恰是最好的解释材料；
> ③ 分成"实体级"和"精确片段级"两层讲，别只报一个数；
> ④ **别再说"零行业退化"**——那是小语料上的结论，扩容后贵金属回落了（6.08%→5.82%），已经在文档里更正的旧说法不要再用。

---

# 第 5 节 · 高频深问 + 答题骨架

## A 组 · 链路与原理（最基础，答不好直接出局）

**A1. 你的 chunk 怎么切的？为什么是 520 字？**
> "基于标题层级切，不是按固定长度硬切。我先用 PyMuPDF 两遍扫描拿到标题层级，然后维护一个标题栈：遇到下一级标题就把当前块 flush 出去，同一小节的相邻段落聚合成一个语义块。单块上限 520 字，是因为 bge-small-zh 的窗口是 512 token，要留余量。下限 120 字，太短的片段信息量不够、还容易噪声。
> 另外每个片段我都带了**标题链前缀**（比如【第一章 经营情况 > 成本分析】），这样编码时章节语义也进去了，命中率会更高。"

**A2. 双语年报的英文噪声怎么处理？**
> "用中文占比做过滤：`中文字符数 / 去掉空白后的总字符数`，低于 45% 的长块直接丢。这条线专门用来干掉双语年报里的英文目录、页眉页脚和免责声明。这也算是'一刀切但有效'的启发式，比上语言检测模型轻得多。"

**A3. 为什么分 4 个 collection，不用一个大的？**
> "三个理由：① **缩小检索域、提升精度**——问白酒不会召回黄金的资料，跨行业噪声天然隔绝；② **便于增量更新**——白酒公司发新年报只重建白酒库，不影响其他库；③ 对应我们原设计里'按行业分账号管理知识库'的多租户隔离思路。
> 代价是路由要准。这块我目前是关键词路由，**是个已知短板**——问'酱香科技'会落到通用库，因为认不出这是白酒。要做对得上 LLM 意图分类或关键词扩表。"

**A4. 为什么召回 100 再精排到 5，直接取 5 不行吗？**
> "因为'最像的'和'最该用的'不一定是同一批。同一家公司上百个片段长得极像，纯向量相关性打分容易把通用模板段排在具体段前面。多召回再精排，是给精排器一个修正机会。
> 但我也要说明：这个思路有个前提——**标准答案得在候选里**。我实测有约 63% 的标准片段进不了 Top-100，那重排再多也救不回来。这是这个方案的结构性上限。"

**A5. bge 编码查询时为什么必须加指令前缀？**
> "bge 在指令微调时就是'带指令编码查询、不带指令编码文档'训练的。不按它训练的姿势用，查询和文档的向量分布会对不齐，召回明显变差。这是很典型的'工具要按说明书用'——**离线入库和在线查询两侧的编码口径必须一致**，这也是 RAG 最常见的翻车点之一。"

**A6. 余弦相似度你怎么算的？**
> "向量入库前做了 L2 归一化，所以余弦相似度等价于点积，省掉一次开方。另外要注意一个细节：**Chroma 存的是距离不是相似度**，要 `1 - distance` 才是相似度，这个转换写错整个指标体系就全乱了。"

**A7. 重排你用的什么？为什么不用 cross-encoder？**（这题现在是加分题，因为有个转折）
> "**先承认：我第一版选的 late-interaction，选它的理由其实不成立。**
> 当时 bge-reranker-base 拉不下来——HF 限速 256MB 要 11 分钟，中途杀进程还留下损坏的缓存。所以我在代码里留了双后端（优先 cross-encoder、失败回退 late-interaction），实际跑起来是 late-interaction。**问题是我后来把'实际跑起来的那个'当成了结论，在文档里写成'在这个规模下 late-interaction 是更实际的选择'——这等于把环境约束包装成了技术判断。**
>
> 后来我把它查清楚了：缓存里那份文件其实是 **ONNX 格式**（既不是 safetensors 也不是 pytorch_model.bin），所以 sentence-transformers 的 CrossEncoder 永远加载不了；我改走 onnxruntime 直接推理就绕过去了。
>
> 然后做了三路受控对照——同一批查询、同一个 Top-100 候选池、同一套命中判定，只换最后一步排序器：纯向量 **6%**、late-interaction **10%**、**真 cross-encoder 17%**，而且四个行业的排序完全一致。
>
> **技术上的区别**：cross-encoder 把 query 和 doc 拼起来过一遍完整模型，能建模细粒度交互，打分最准，代价是每个候选一次前向、延迟高；late-interaction 是离线 token 表征上做 MaxSim，快、但精度次一档。**所以正确的说法是：late-interaction 是延迟受限时的折中，不是首选；我把它当结论用是错的。**
>
> 但我必须说明：这个对照每个行业只抽 25 条、总共 100 条，**绝对幅度不能当结论用**，要变成主指标得全量重跑 2,754 条。所以我不会说'精确命中率提升了一倍多'，我只能说方向是明确的、下一步该做什么是明确的。"

**A8. 你的检索为什么会返回空？怎么保证不空？**（这题能答好非常加分）
> "这是我踩得最狠的坑，也是我后来写了很多防御代码的原因。现象是**本地正常、云端返回空**。根因有三个叠在一起：
> ① 云端集合结构可能是 list 而不是 dict，我一开始直接 `.items()` 就抛异常；
> ② 查询 key 不一定是字符串，出现 int 或 list 时，`字符 in 字符串` 这种比较会抛 TypeError，被外层 except 吞掉；
> ③ 最隐蔽的一个：我早期写的是'找到重叠度最高的那个查询就直接返回它的结果'——如果那个查询恰好映射到空列表，整体就返回空了。
>
> 修法是三条：**通用递归收集器**（不假设结构，递归下钻找命中）、**查询 key 强转 str + 全程 try/except**、**把整个检索域的命中收集成列表统一排序去重取 Top-K**（绝不因为单个查询为空而整体返回空）。外面再套四层兜底：本行业 → 跨行业 → 全量强制兜底 → 异常时再兜一次。
>
> 还有一个排查教训值得说：我一度把'检索空'误报成'知识库未加载'，因为告警文案里写的变量和判断条件用的不是同一个变量。后来我定了条规矩——**'资源已加载'和'检索有命中'必须分开判断、分开报错**。"

## B 组 · 评测与指标（RAG 岗的核心考点）

**B1. 为什么不用人工写的问题集？**
> "两个原因：一是**指标会虚高**——问题是我自己写的，自然会挑系统答得好的；二是**相关性判定会退化成字符串匹配**，那不是真正的相关性。
> 换成 chunk-derived 后，查询是从真实语料反向生成的，我没法挑题，也没法改标准答案。这套方法和 BEIR / MS MARCO / RAGAS 的测试集构造同源。"

**B2. chunk-derived 具体怎么造？qrels 怎么定？**
> "三步：① 从向量库里筛合格片段——散文型、长度 60 到 800 字、含数字、命中一个 125 词的金融关键词表，避免把纯目录、纯表格当语料；② 为每个片段反向合成一个'答案就在这段里'的问句，合成是确定性的抽取式（实体 + 指标 + 年份掩码），保证可复现、不依赖网络；③ **片段所属的公司或政策主题就是 gold qrels**。
> 按查询文本去重之后，2,754 条。"

**B3. 相关性为什么按"实体级"判，不判具体片段？**
> "这是 RAGAS 的思路——**entity / document level context recall**。因为 RAG 的下游是 LLM 生成，同公司的另一段有用信息通常同样能支撑回答，不该判为不相关。同时我用分级相关性算 NDCG：精确命中源片段算 2 分、同主体其他片段算 1 分，这样排序质量仍能被刻画。
> 然后我**单独设了一个'精确片段命中率'指标做诊断**——它不参与'系统好不好'的结论，专门用来暴露排序特异性问题。这也是为什么我能把 96.3% 和 7.6% 两个数摆在一起讲而不矛盾。"

**B4. Recall@5 和 Precision@5 这么高，是不是评测太简单了？**
> "我先承认确实存在'实体级判定偏宽'的可能，因为一个主体下通常有几十到上百个片段，命中的机会变多了。
> 但我做了两个交叉验证：① **分行业看**，宏观只有 86.6%，如果是评测太松，不该有明显短板；② **精确片段命中率只有 7.6%**，说明指标体系里保留了很强的区分度。所以我的判断是：**实体级召回确实强是真结论，但'强'的边界是'找对公司'，不是'找对段落'。**"

**B5. 生成质量怎么评的？LLM-as-Judge 可靠吗？**
> "用与线上专家智能体**完全相同**的 system/user prompt、拼同一批 Top-5 上下文生成答案，再让 deepseek-chat 当裁判按 RAGAS 三项打 0~1 分：忠实度、答案相关性、上下文利用率。
> **对裁判可靠性的处理**：① 三项分开打、每项给打分口径，不做单一总分；② 我同时保留了**确定性离线代理指标**（比如答案相关性代理 = 问题词项在上下文中的覆盖率），两条线互相印证，发现代理值明显偏低但方向一致，说明 LLM 裁判没有系统性放水；③ 模式和模型名（`mode: llm`、`judge_model: deepseek-chat`）都记进产物里，**不用假数字冒充真值**。
> 我也要诚实说局限：**LLM-as-Judge 存在偏置，同族模型自评会偏乐观。** 要做严格评测，得人工抽检校准，或者引入不同族模型的裁判做一致性检验。"

**B6. 忠实度 0.90 意味着什么？剩下 10% 是什么？**
> "我理解它更像'**溯源覆盖率**'而不是严格的幻觉率——0.90 大致意味着九成表述能在检索资料里找到依据。
> 剩下那部分我归成三类：① 模型的常识性补充（比如'白酒行业受消费升级影响'这类不来自片段但也不算错的表述）；② 多片段推断（把两段信息合成一个新结论，裁判可能判为无直接依据）；③ 真的越界表述。第 ③ 类才是要根治的，前两类需要跟裁判口径对齐。"

**B7. 为什么宏观行业最弱？**
> "检索侧宏观 Recall@5 只有 86.6%，忠实度也是最低的 0.841，两处一致，说明是真实短板。原因我判断是：**政策文本的措辞高度抽象、主题分散**，同一份货币政策报告里能覆盖很多话题，所以查询和片段的语义对应关系比公司财报弱得多。
> 有意思的是宏观的**精确片段命中率反而是最高**的 11.2%——因为政策报告单篇短、同质度低、'兄弟片段'少，反而容易精确定位。**这两个数字一起看，恰好印证了我的核心判断：精确命中率低的主因是同公司片段同质化，不是检索器差。**"

## C 组 · 工程与取舍（区分"做过"和"做明白"）

**C1. 你线上是实时检索吗？**（⚠️ 必答陷阱题，见第 6 节）

**C2. 知识库怎么更新？增量怎么做？**
> "设计上是**按行业分库**，所以增量更新的粒度是行业级：某家公司发了新年报，只需要重新解析这份文件、重新切分、把该行业的 collection 重建一次，其他三个库不动。
> 我目前的做法是删除后重建 collection（`delete_collection` + `create_collection`）。要做得更细，可以按 `chunk_id` 前缀（我用了 `{行业}_{文件名}_{序号}`）做**文件级增删**：先按 source 元数据删掉该文件的旧片段，再插入新的。再往上一步是加**版本号和生效时间元数据**做时间过滤，避免用旧年报回答新问题。
> 另外每次重建都要**重跑评测**，避免静默退化——这是我这套评测最大的用处。"

**C3. 成本、延迟怎么权衡？**
> "几个明显的杠杆：① **离线预计算**——token 表征一次算好，查询侧只编码一次 query，这是我把 2,754 条查询从 20 分钟压到 2 分钟的关键；② **两段式召回**——先便宜地召回 100，再用较贵的方法精排到 5，避免对全库做重排；③ **按行业路由**把检索域从 12,594 缩到 2,000~5,000 量级；④ 生成侧用**流式输出**改善体感延迟。
> 还有个部署侧的取舍：为了让线上启动快、体积小，我把检索结果做成了**离线快照**，应用侧不装 chromadb 和 torch。代价我也清楚——见 C1。"

**C4. 多智能体工作流里，RAG 是怎么接进去的？**
> "我把检索封成了一个**独立纯模块** `kb.py`，只依赖 os / json，不依赖 Streamlit，然后工作流里的检索节点直接 `from kb import` 调用。
> **为什么不用依赖注入？** 因为我踩过坑：最初是通过图的状态传一个检索函数进去（`state["rag_fn"]`），本地正常，云端拿到的是 `None` → 静默返回空结果。根因是 LangGraph 的 channel schema 对 `Callable` 类型在不同版本下不一定传播。
> 结论就是：**别把编排框架当依赖注入通道。**共享逻辑抽成纯模块、节点内直接 import 才稳；注入只保留作为单测入口。"

**C5. 如果知识库里没有答案，你的系统会怎样？**
> "三道防线：① 约束式 Prompt 明确要求'仅依据参考资料作答、不得超范围，无依据时说明未知'；② 检索侧有四层兜底，但兜底给的是'最相关的东西'而不是'正确答案'——所以关键还是 Prompt 约束；③ 生成后面还有一个负责核验信源、判断是否存在幻觉的智能体。
> 不过要诚实地说：**'知识库里没有'这种情况我目前没有专门的拒答机制去覆盖**（比如相似度低于阈值就直接拒绝回答）。我现在的做法是调高 Prompt 的约束强度。要做得更稳，应该加一个**检索置信度门限**——Top-1 相似度低于某个值时直接返回'资料不足'。这是个明确的待补项。"

**C6. 你怎么保证本地和线上行为一致？**
> "这个坑我吃过。除了检索结构的防御式处理，我还做了两件事：① **构建指纹**——把检索函数的版本号写进所有状态文案里，线上报错时一眼能看出它跑的是哪一版；② **应用内自诊断面板**——把文件路径、是否存在、大小、JSON 能否解析、顶层键、加载失败原因一次性摊开显示，不用去翻云端日志。
> 还有一个版本坑：`requirements.txt` 里 streamlit 不加上限会被装到最新版，某个 API 在新版被移除了，线上直接崩。所以依赖要**加版本上限**。"

**C7. 这个规模（13k 片段）还用得上向量库吗？**
> "13k 的量级，暴力算余弦其实也很快，所以工程上完全可以不上向量库。我上 Chroma 主要考虑三点：① 我要的是**四库隔离**和 metadata 过滤能力，这比检索速度更重要；② **便于增量更新**和维护；③ 这是**真实 RAG 系统的标准形态**，用它能暴露真实问题——比如这次得到的'结构假设导致空结果''距离与相似度混淆'这些坑，纯暴力检索根本遇不到。
> 但这个问题问得对：如果数据量再小、而且不需要 metadata 过滤，我会直接上 numpy 矩阵乘，少一个依赖。"

## D 组 · 开放与展望（看出你有没有全局视野）

**D1. 让你重做一遍，你会改什么？**
> "三个优先级最高的：
> ① **先把重排器换成真 cross-encoder 并全量重跑**——我三路对照已经证明候选池内部还有空间（17% vs 10%），但只跑了 100 条，得先把这个数字落到 2,754 条上，这是最便宜、最确定的一步；
> ② **换 embedding 并重嵌全库**——这是我算出来的最大杠杆，因为 63% 的标准片段进不了 Top-100，覆盖率才是天花板；
> ③ **加查询改写/扩展**——让模糊问题的目标片段能进入候选，成本比换模型低，收益可能更直接。
> 还有一个流程上的改进：**把评测变成 CI**，每次改切分或换模型都自动重跑 2,754 条，防止静默退化——这次的贵金属回落就是靠重跑才发现的。"

**D2. 如果数据量涨到百万级，你的方案哪里会崩？**
> "会崩在三处：① **单库暴力语义检索的排序成本**上升，需要真正的 HNSW 索引和分片；② **collection 粒度**要重新设计——现在是 4 个行业库，百万级下需要按公司/时间二级分区，否则单库仍然过大；③ **评测成本**——2,754 条 × Top-100 的重排已是我当前提速后的极限，百万级必须改成抽样评测 + 小规模全量回归两手抓。
> 另外百万级下 **metadata 过滤 + 向量检索的混合查询**会变成核心能力，我现在基本只靠'行业路由'这一层过滤，太粗。"

**D3. 你觉得 RAG 现在最大的问题是什么？**
> "我的体感是**评测**——不是检索不准，而是**很多人不知道自己准不准**。常见的是拿人工小样本问题集测出一堆漂亮数字，但那既不能反映真实分布，也无法定位失败模式。
> 第二个是**检索覆盖率被忽视**：大家容易把精力放在重排上（因为重排很酷、有效果可展示），但我的实验说明要分两层看——**候选池内部还有空间（换更强的重排器就能拿到），可一旦目标片段压根没进候选，重排做得再好都是零收益。先看覆盖率，再谈排序。**"

**D4. 未来 RAG 会被长上下文模型取代吗？**
> "我的判断是**不会被完全取代，但边界会变**。长上下文解决的是'能不能塞进去'，解决不了三个问题：① **成本**——每次把 13k 片段塞进上下文是不现实的；② **可溯源**——不检索就没有明确的出处，而金融场景必须能标到 PDF 页码；③ **权限与隔离**——多租户场景下必须按账号过滤能看的数据，这是检索层的职责。
> 但长上下文会**压缩掉'简单检索'的空间**：如果只是从几篇文档里找答案，确实可以不用向量库。RAG 的价值会往'大规模、要溯源、有权限、要控成本'这些场景收敛。"

---

# 第 6 节 · 三个陷阱题的标准答法

这三题是专门用来"戳破"的，答得好是加分，答不好是重伤。**原则是：第一时间承认，然后立刻转成工程取舍的讨论。**

## 陷阱 1 · "所以你线上其实没做真检索？"

**先承认，别绕**：
> "对，**线上演示读的是一份离线检索快照**，不是实时向量检索。
> 具体做法是：我拿 2,754 个查询在真实环境里跑了一遍完整检索和重排，把结果——每问召回了哪 5 个片段、相似度多少、rerank 分多少、出自哪份文件哪一页——序列化成一个 JSON，线上直接从文件读。"

**然后讲取舍**：
> "这么做的原因是部署侧：**Streamlit Cloud 上装 torch + chromadb 会让镜像大几百 MB、冷启动从几秒变几十秒**。做成快照后依赖只剩 os 和 json，启动快、体积小，而且**断网也能完整演示**。"

**接着讲代价（这句最关键，主动说）**：
> "代价我很清楚：**自由输入的问题走的是'最近邻预置查询'匹配**——拿用户问题去和那 2,754 个预置查询做字符重叠度比对，返回最接近那组的结果。如果问题跟预置集差异很大，相关性会下降。**这是 Demo 形态的取巧，真实检索能力是由离线脚本实跑证明的。**"
> "如果这是一个真上线的产品，我会改成**云端实时检索**——用轻量向量库加量化索引控制体积，快照只留作断网降级方案。本质上这是个**演示成本优先**的选择，不是技术能力问题。"

## 陷阱 2 · "精确命中率才 7.6%，那你的检索是不是不行？"

> "**7.6% 是真实值，我不打算修饰它。**但我建议把它和另外两个数一起看：实体级 Recall@5 是 96.3%、生成忠实度是 0.90。
> 它的含义是：**系统很擅长帮你找到'对的那家公司'，但不擅长精确到'对的那一段'。**
> 而且我做了归因，不是拍脑袋判断：未命中的查询里 **86.6% 的 Top-5 至少包含一个同公司片段**，Top-5 里平均 3.98 条来自同一家公司。说明失败模式是**实体内片段互挤**，不是跨实体找错。
> 我甚至还找到了反例佐证：政策类资料的精确命中率反而是最高的 17.6%，因为政策报告单篇短、同质度低。**如果是检索器本身差，不会有这种结构性差异。**"

## 陷阱 3 · "这个测试集是你自己造的吧？"

> "是自建的，但**不是自己出题自己判卷**——这两者差别很大。
> 我做的是业界标准的 chunk-derived：**查询是从语料反向合成的，标准答案是片段所属的公司，我没法挑简单题、也没法改答案**。这套构造方法在 BEIR、MS MARCO、RAGAS 的测试集里是通用的。
> 而且我特意保留了**不可作弊的方向性证据**：如果评测偏松，不该出现宏观只有 86.6% 这种明显短板，也不该出现精确命中率 7.6% 这种难看数字。**能暴露自己缺点的评测，通常比全绿的评测更可信。**"

---

# 第 7 节 · 简历怎么写这一条

**一版可直接用的条目**（建议放最前，因为是主项目）：

> **金融行业 RAG 知识库 · 主要实现者**
> - 独立搭建 152 份公开年报/央行报告的离线建库链路：PyMuPDF 两遍扫描按字号识别标题层级 → 基于标题栈的语义切分（520 字上限、中文占比 45% 过滤双语噪声）→ bge-small-zh 编码 + L2 归一化 → 按行业分 4 个 Chroma collection，共 12,594 个片段
> - 设计不可作弊的检索评测：采用业界标准 chunk-derived 方法（查询由语料反向合成、实体级 qrels）构造 2,754 条查询；实测 Recall@5 **96.3%**、NDCG@5 **0.94**，同时以精确片段命中率 **7.6%** 诚实暴露排序特异性缺陷，并完成归因（86.6% 未命中含同公司片段）
> - 实现两段式检索与重排：bge 召回 Top-100 → 混合重排（0.6×bi-encoder 余弦 + 0.4×late-interaction）取 Top-5，精确命中率 7.27% → **7.59%**；通过预计算 token 表征将全量评测耗时从 20+ 分钟降至 **约 2 分钟**
> - 设计重排器三路受控对照实验（纯向量 / late-interaction / 真 cross-encoder，ONNX 推理，同候选池同判定口径）：四个行业排序一致，真 cross-encoder 精确命中率 **17%** vs late-interaction 10% vs 基线 6%，据此修正上线方案的选型依据
> - 通过排名分布分析定位真实瓶颈：**约 63% 的 gold 片段落在 Top-100 候选之外**，论证「候选池内为重排可优化空间、候选池外为检索覆盖率天花板」的两层归因
> - 以 DeepSeek LLM-as-Judge 完成 RAGAS 对齐的生成端评测：忠实度 **0.90** / 答案相关性 0.87 / 上下文利用率 0.87；并为检索模块加入四级兜底与构建指纹，消除"本地正常、云端空"的静默故障

**写法要点**：
- 每条都是**"动作 + 方法 + 数字"**，没有一句"参与了""负责了"；
- 主动写出 7.6% 这个难看的数字 → **反而让面试官相信其他数字**；
- 最后一条"消除静默故障"是压舱石，说明你不只会调包，还能做生产级加固。

---

# 第 8 节 · 你要反问面试官的问题（展现 RAG 视角）

选中两个问就行，别多：

1. **"咱们现在的检索评测是怎么做的？有线上指标吗，还是主要靠离线测试集？"**
   —— 一问就知道团队是否真的在管 RAG 质量。很多团队这块是空的，你等于顺手展示了自己的强项。

2. **"知识库的更新频率大概是什么量级？是定期全量重建，还是走增量？"**
   —— 显示你关心的是长期运营而不是一次性搭建。

3. **"现在这套 RAG 的主要瓶颈，团队判断是在检索侧还是在生成侧？"**
   —— 这题很专业：**如果对方答不出，说明他们没做过归因**；如果答得出，你立刻能接住深入聊。

4. **"如果我负责这块，前三个月您最希望我解决的是哪个问题？"**
   —— 把话题变成"我来了能干什么"，落到具体价值上。

---

# 第 9 节 · 十条"别说"清单

| 别说 | 为什么 | 改成 |
|---|---|---|
| "我们团队做了……" | 面试官听不出你干了什么 | "我负责的是……" |
| "效果挺好的" | 没有量化就是没做过 | 直接报数字 |
| "我们的指标是 96%" | 只报好看的那个会被追问到崩 | 主动报两个：96.3% 和 7.6% |
| "重排提升了效果" | 只报提升会显得没量级感 | "提升 0.32pt，基本算失败，但它让我算清了瓶颈在哪" |
| "我选 late-interaction 是因为它更合适" | 这是把环境约束包装成技术判断，会被追问穿 | "当初选它是因为 cross-encoder 下载不下来；后来补做对照发现它差一档，这是我自己的判断失误" |
| "cross-encoder 能把精确命中率翻倍" | 只有 100 条样本，属过度声称 | "小样本对照里它 17% vs 10%，方向明确；幅度要全量重跑才能给" |
| "用了 RAG 就减少了幻觉" | 太笼统，且不成立 | "忠实度实测 0.90，剩下 10% 我归成三类" |
| "调了一下参数" | 说明你不知道参数为什么是那个值 | 说出参数 + 依据（512 token 窗口 → 520 字上限） |
| "那是数据的问题" | 甩锅 | 说出失效模式和占比（86.6% 含同公司片段） |
| "知识库很干净" | 会被追问噪声处理 | 说中文占比过滤 + 纯表格/目录排除 |
| "还没想过这个问题" | 单次可以，多了就掉分 | "目前没做，我的思路是……" |
| "我们用了最新的 XXX 模型" | 堆名词不加分 | 说清为什么用它、代价是什么 |

---

# 第 10 节 · 一分钟背稿（打印版）

> 我主要做 RAG，方向是金融垂直领域的知识库。
>
> 离线侧：152 份公开年报和央行报告，我用 PyMuPDF 两遍扫描——第一遍统计正文字号中位数，第二遍靠"字号大于正文 1.22 倍且行很短"识别标题层级；然后基于标题栈做语义切分，单块 520 字上限，用中文占比 45% 过滤双语年报的英文噪声；最后 bge 编码 + L2 归一化，按行业分 4 个向量库，一共 12,594 个片段。
>
> 在线侧：行业路由 → 查询编码（带 bge 指令前缀）→ 召回 Top-100 → 混合重排取 Top-5 → 约束式 Prompt 生成并标注来源。
>
> 我做得最深的是评测。我弃用了人工问题集，改用业界的 chunk-derived 方法——从真实片段反向合成 2,754 条问句，片段所属公司就是标准答案，这样我没法挑题也没法改答案。
>
> 结果是两个数：**实体级 Recall@5 是 96.3%，但精确命中源片段只有 7.6%。** 我做了归因：未命中的查询里 86.6% 的 Top-5 至少含一个同公司片段，说明是实体内片段互挤，不是找错公司。
>
> 重排我做了两轮实验。第一轮上 late-interaction，全局只涨 0.32 个点，基本算失败——但它让我算清了正确的判断：**有 63% 的标准片段根本不在 Top-100 候选里，所以重排只能在候选池内部起作用，天花板被检索覆盖率锁死。**
>
> 第二轮我回头质疑了自己的选型：当初不用 cross-encoder，理由是"模型下载不下来"——那是环境约束，不是技术结论，我却把它写成了结论。我把那份模型转成 ONNX 推理，做了三路对照，100 条查询下结果是 6% / 10% / 17%，**四个行业排序完全一致，真 cross-encoder 胜出**。样本还小、幅度要全量重跑确认，但它至少说明我当初那个"够用就行"的判断是错的，而且是我自己查出来的。
>
> 这个项目对我最大的价值，是让我知道了 RAG 的评测该怎么设计才不可作弊，以及一个检索系统的瓶颈该怎么定位。

---

*本文档所有数字与结论与《FinSynagent_三大模块环节详解.md》《FinSynagent_三大模块_零基础通俗讲解.md》完全一致，可直接交叉核对。*
