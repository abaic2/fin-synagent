# Fin Synagent 面试项目讲解底稿 · 详细版（零基础可讲 + 能接深问）

> **给谁看**：你自己。你对这套技术还不熟，但面试要拿它当项目讲，且要能接住深挖。
> **怎么用**：① 先读「第一节扫盲」把名词搞懂；② 背「第二节演讲 + 第三节讲法」；③ 用「第五节面试题」做模拟问答；④ 面试官深挖时，翻「第四节踩坑」和「附录技术速查」找数字和细节。
> **原则**：每个技术名词第一次出现都有"大白话 + 生活类比"；深入部分标注「追」= 面试官常追问的点。

---

## 第一节：核心概念扫盲（不懂先背，讲时可用类比）

### 1. 大模型 / LLM（如 DeepSeek、星火、GPT）
- **大白话**：一个"读过几乎所有书、能聊天写作的超级文本生成器"。
- **类比**：记忆力超强、什么都能聊的助理。
- **两个致命缺点（面试必提）**：① **幻觉**——会一本正经地胡说；② **知识截止 + 无私有数据**——不知道你最新的、内部的资料。
- **追**：为什么不直接用 GPT 做投顾？→ 通用模型不懂你的私有研报、且回答不可溯源、还有合规风险，所以要接 RAG + 做成可控流程。

### 2. RAG（检索增强生成，Retrieval-Augmented Generation）
- **大白话**：让模型回答前，先去你的资料库翻出相关段落，"贴在手边"再开口。
- **类比**：**开卷考试**——不是靠背，是考场上允许翻书（你的知识库）。
- **解决什么**：① 模型不知道私有资料 → 现在能查；② 编造假信息 → 现在要求"说话有出处"。
- **追**：RAG 的具体步骤？→ 离线建库（切分→向量化→入库）+ 在线查询（问题向量化→相似度检索 Top-K→拼进 prompt）。详见附录 A。

### 3. 多智能体（Multi-Agent）
- **大白话**：不让孩子一个人干完所有活，而是组**小团队分工**——有人拆任务、有人查资料、有人写初稿、有人挑毛病、有人核对事实。
- **类比**：一家**小型投顾公司**——老板拆解需求、研究员查资料、分析师写报告、合规挑错、终审核对信源。
- **为什么比"一个人"好**：单人易遗漏、过程黑箱；团队分工 + 互相挑错，质量更高也更透明、可追责。

### 4. System-2 深思熟虑（慢思考）
- **大白话**：心理学"快思考（直觉）vs 慢思考（逐步推理）"。系统故意**慢下来**，把每一步思考展示出来，而不是秒回一句话。
- **类比**：与其脱口"买！"，不如摊开草稿纸一步步算给你看。
- **追**：这和 Chain-of-Thought（思维链）啥关系？→ CoT 是"让模型一步步想"，System-2 是我们**在工程上把每一步拆成独立可见的 agent 节点**，比一段 CoT 文本更可控、可干预。

### 5. LangGraph（智能体编排框架）
- **大白话**：把"团队 workflow"画成一张**带判断的流程图**。
- **类比**：**流程图软件**——方框=步骤（节点），箭头=顺序；还能画"不合格就退回重做"的回环。
- **关键点**：节点（Node）、边（Edge）、**条件边（Conditional Edge，按判断选下一步）**、**通道Reducer（把多轮输出累加，如把每轮轨迹 append 起来）**。它让"批评—修订"回环可控、可复现。
- **追**：和普通 Chain（链式调用）区别？→ Chain 是写死的 A→B→C 直线；LangGraph 支持**循环、分支、状态共享**，适合"批评—修订"这类需要回环的场景。

### 6. 微调 / LoRA（让模型变"金融专精"）
- **大白话**：通用大模型是"通才"，在金融语料上补一门"专业课"，用很少改动就能专精。
- **类比**：通才去考一张**金融证书**——不用重读大学，只补考点。
- **LoRA**："只改模型极小一部分参数"的省钱微调法（原模型冻结，只训低秩适配矩阵）。
- **追**：RAG 和微调怎么选？→ 见 Q2。本项目以 RAG 为主、LoRA 为辅（可选）。

---

## 第二节：电梯演讲（三个长度，按面试节奏选）

### 30 秒版（开场照背）
> 「我做了一个叫 **Fin Synagent** 的项目，是一个**基于大语言模型的多智能体投顾助手**。一句话说，它把专业投顾分析一只股票/一个行业的过程，拆成一套**看得见、可追责的多智能体流水线**：先拆解问题、再查知识库找依据、专家写分析、评论家挑错并退回重改、最后核对信源才给结论。我这么做是为了解决两个痛点：普通人看不懂研报；大模型回答不透明、还会瞎编。技术上用了 **RAG 知识库 + LangGraph 编排 + 星火/DeepSeek 推理**，还做了一套**诚实的评测体系**。它既能演示，也是一套可复用的多智能体方法论。」

### 1 分钟版（补一句架构 + 两功能子模块）
> 「技术上分四层：交互层是 Streamlit 多页 Web 应用；能力层有 **Consult 智能咨询**和 **Screen 智能荐股**两大功能——Consult 用 **LangGraph 把 7 个智能体串成流水线**（领导拆解→RAG 检索→专家作答→评论家挑错→修订回环→求证→总结），Screen 用一棵 **12 节点的筛选树**（拉行情→意图解析→建股票池→抓股吧评论→基本面/技术面/情绪面/行业面四维特征→汇聚评分→生成理由→反思校验）自动挑 Top-3；编排层用 LangGraph 把智能体画成状态图、支持批评—修订回环；底座层是星火/DeepSeek 推理 + 行业 RAG 知识库 + 可选 LoRA 微调。评测上我没把指标刷成 100%——改用业界标准的 chunk-derived 自建 benchmark（从知识库真实 chunk 反向生成 259 条查询，来源主体即标准答案 qrels，与 BEIR / RAGAS 同源），实跑 Recall@5≈95%、NDCG@5≈0.925 的强检索，但「精确 chunk 命中率」仅≈13%，诚实暴露排序特异性缺陷。」

### 3 分钟版（完整走一遍，见第三节）

---

## 第三节：项目讲法（详细，按此顺序讲，能接深问）

### 3.1 痛点（为什么做）
- 普通人看不懂研报、抓不住逻辑。
- 直接问大模型：回答黑箱、会幻觉、信源不透明、且不懂私有/最新资料。

### 3.2 我的解法（三层）
1. **过程工程化**：把分析拆成 6 个智能体（Leader / RAG / Expert / Critic / Verify / Summary），每步可见、可干预。
2. **事实佐证**：接行业知识库（RAG），回答必须引用来源片段。
3. **自我纠错 + 求证**：引入"评论家—修订"回环 + 显式信源核验，压住幻觉。

### 3.3 系统数据流（讲清"一句话怎么变成一份报告"）
```
用户问题
  → Leader 拆解（子任务 + 分配专家角色）
  → RAG 检索（问题向量化 → 行业库 Top-K 召回 → 片段注入）
  → Expert 作答（基于片段写结构化分析）
  → Critic 审查（给批评/判定 revise or pass）
  → [若 revise] Revise 修订 → 回到 Critic（回环，最多 N 轮）
  → Verify 求证（列信源、交叉核验）
  → Summary 总结（可执行结论 + 邀请追问）
```
- **追**：回环怎么不无限循环？→ 用计数器 `loop < max_loop`，且**计数放在 Revise 节点内、只在 revise 时 +1**（踩坑见 4.1）。

### 3.4 两大功能（**两个都要讲，各子模块拆开讲**）

前面 §3.3 是 Consult 的整体数据流，这一节把**两个功能各自的内部子模块**拆开讲透。讲的时候建议：先说一句"整个系统有两大功能"，再各用 1 分钟依次过子模块，面试官会觉得你结构清晰、真做过。

---

#### 3.4.A 功能一 · Consult 智能咨询（7 个智能体子模块）

**一句话定位**：用户用大白话问"白酒板块今年还能不能配 / 五粮液估值贵不贵"，系统不秒回一句话，而是把**拆解、查资料、写稿、挑错、核实、总结**的全过程摊开给你看。底层用 LangGraph 把 7 个智能体串成一张状态图（代码见 `agent_graph.py`）。

**整体数据流（7 节点 + 回环）**
```
👔 leader 拆解任务
   → 📚 retrieve 查知识库
   → 🎓 expert 写初稿
   → 🧐 critic 挑毛病 → 判定 revise/pass
        ├─ revise → ✍️ revise 修订 → 回到 critic（回环，最多 N 轮）
        └─ pass  → 🔎 verify 列信源核验 → 📋 summarize 给最终建议
```

**逐个子模块（按面试可讲顺序）**

1. **👔 Leader 领导智能体（leader）**
   - 大白话：把用户的一个大问题拆成几个小任务，并指派"哪个专家来答"。
   - 类比：部门主管接到需求，先写任务清单、再分给对应同事。
   - 产出：`leader`（Markdown 子任务列表 + 分配专家）。真实模式调 LLM，演示模式用内置脚本兜底。

2. **📚 RAG 知识库检索（retrieve）**
   - 大白话：去行业向量库里翻出和问题最相关的前 K 段资料，拼成"手边材料"。
   - 类比：开卷考试时，翻到对应章节、把关键页折角。
   - 产出：`rag_hits`（命中片段）+ `rag_ctx`（"【来源 · 页码】文本"拼接）。**健壮性设计**：优先用注入的检索函数，失败则回退 `kb.get_rag_hits` 直接 import（这就是踩坑 4.4 里"不依赖云端注入、抽到独立纯模块"的落地）；还有 `_iter_hits` 多级兜底，绝不返回空。
   - 追：为什么要有"注入 + 回退 import"两套？→ 因为部署端 langgraph 版本差异可能让注入的 callable 失效，直接 import 是更稳的兜底，对应踩坑 4.4。

3. **🎓 专家智能体（expert）**
   - 大白话：行业分析师，基于"手边材料"写出专业、数据驱动、带风险提示的回答。
   - 产出：`expert`（结构化 Markdown 分析，关键结论加粗）。

4. **🧐 评论家智能体（critic）—— 核心亮点的起点**
   - 大白话：专门挑刺的"质量审核员"，指出遗漏、逻辑漏洞、数据存疑，并给改进建议。
   - 产出：`critic`（批评意见）+ `verdict`（判定 `revise` 或 `pass`）。
   - 判定逻辑：真实模式看评论文本里是否出现"通过/无需修改"等关键词；演示模式用"首轮必 revise、之后 pass"保证至少走一次回环。

5. **✍️ 专家修订（revise）—— 回环体里被重复执行的节点**
   - 大白话：根据评论家的意见把初稿改好。
   - 产出：`expert_revise`（修订后完整要点）+ **`loop + 1`**（循环计数在此 +1，踩坑 4.2 关键！）。

6. **🔎 搜索与求证智能体（verify）**
   - 大白话：事实核查员，列出每条关键结论的出处（研报/数据/新闻），并判断是否可能有幻觉。
   - 价值：把"凭空编"锁死在"有出处"里，是治幻觉的第三道防线。
   - 产出：`verify`（可溯源信源清单 + 真实性判定）。

7. **📋 总结领导（summarize）**
   - 大白话：把全流程（拆解→分析→挑错→修订→求证）汇总成一份"最终投资建议 + 可执行结论"，并邀请用户追问。
   - 产出：`summary`（最终建议）。

**回环机制（必讲）**：critic 判 `revise` 且 `loop < max_loop` 时，经条件边回到 revise，再回 critic，形成"批评—修订"反思循环；否则进 verify。计数放在 revise 节点内、只在 revise 时 +1（详见踩坑 4.2）。

**双模式（加分项）**：**真实模式**接 DeepSeek 实时推理（用户可自带 Key）；**演示模式**内置高质量脚本，断网也能演示。这让系统"既能真跑、又能随时演示"。

---

#### 3.4.B 功能二 · Screen 智能荐股（筛选树 12 个节点）

**一句话定位**：用户用口语说"我想找稳健型的白酒股"，系统用一棵**筛选树**自动筛股——先拉数据、解析意图、建股票池，再从四个维度算特征，最后 LLM 评分挑 Top-3 并给理由。底层同样用 LangGraph（代码见 `screen_graph.py`），共 **12 个节点**，分三层：**数据获取层（4）→ 四维特征层（4）→ 汇聚决策层（4）**。

**整体数据流（12 节点 + 回环）**
```
📡 fetch_data 拉行情
 → 🧭 planner 解析意图
 → 🏗️ build_pool 建候选池
 → 🌐 fetch_comments 抓股吧评论
 → 💰 feat_fundamental → 📈 feat_technical → 💬 feat_sentiment → 🏭 feat_industry
 → 🧬 synthesize 四维汇聚
 → ⚖️ scorer 加权评分 → 🤖 reasoner 生成观点与理由
 → 🧐 critic 校验 → 判定 revise/pass（revise 则回 reasoner 重生成）
```

**① 数据获取层（4 个子模块）**

1. **📡 实时行情获取（fetch_data）**
   - 大白话：去行情接口拉取每只候选股的报价和近 120 天日 K 线。
   - 真实模式调东方财富接口（带 1 次退避重试，失败回退 Yahoo）；演示模式用内置示例数据。
   - 产出：`codes / rt / klines / rt_src`。

2. **🧭 意图解析（planner）**
   - 大白话：把口语需求（"稳健型白酒"）翻译成机器能用的结构化筛选条件 JSON。
   - 类比：前台把顾客的话转成工单字段。
   - 产出：`intent = {sector, risk_preference（low/medium/high）, objective}`。风险偏好做映射（保守→low、稳健→medium、积极→high）。

3. **🏗️ 股票池构建（build_pool）**
   - 大白话：先按行业圈定范围，再按基础门槛（**市值 > 500 亿、非 ST**）过滤，得到干净候选池。
   - 产出：`pool`（候选股列表，例：白酒池约 5 支）。
   - 追：为什么用"市值>500亿、非ST"这种硬门槛？→ 先排除仙股/ST 暴雷股，保证候选都是流动性好、质地相对稳的龙头，符合"稳健"诉求。

4. **🌐 个股评论抓取（fetch_comments）**
   - 大白话：真实模式下，用线程池（ThreadPoolExecutor）按股票代码**并发**爬取东方财富股吧的真实散户评论（每只约 15 条）。
   - 价值：给"情绪面"提供真实语料（散户在骂还是吹）。演示模式跳过。
   - 追：为什么并发？→ 多只股票串行太慢，线程池把等待时间叠起来、整体提速；单只失败自动降级为空，不影响其他。

**② 四维特征层（4 个子模块，对应投资分析的四个角度）**

5. **💰 基本面特征（feat_fundamental）**：提取 **PE / PB / ROE / 营收增速**，筛选逻辑"低估值 + 高 ROE + 稳定增长"。
   - 类比：看一家公司"赚不赚钱、贵不贵、成长快不快"。

6. **📈 技术面特征（feat_technical）**：提取 **趋势 / 均线形态 / 波动率 / MACD**；真实模式由真实日 K 计算 MA20/60 与 MACD。
   - 类比：看股价"走势图"，判断现在是上升通道还是破位。

7. **💬 情绪面特征（feat_sentiment）**：用 **FinBERT 或 DeepSeek** 做情绪三分类（正/中/负），并算"带符号情绪均值"，还能把每只股票的逐条评论聚合，粒度更细。
   - 类比：看股吧里散户是乐观还是恐慌，作为反向/顺势参考。
   - 追：情绪面为什么重要？→ 散户情绪常领先或放大短期波动，纳入可让推荐更"接地气"。

8. **🏭 行业面特征（feat_industry）**：提取**宏观特征（货币政策等）+ 行业景气特征**，来自预置的行业知识。
   - 类比：看"这个赛道整体景气不景气、政策风向如何"。

**③ 汇聚决策层（4 个子模块）**

9. **🧬 四维特征汇聚（synthesize）**
   - 大白话：把上面四个分支算出的特征，像树状收束一样汇总成"一只股票的综合特征向量"，喂给评分。
   - 类比：四个部门各交一份报告，汇总成一份总表。

10. **⚖️ LLM 综合评分（scorer）**
    - 大白话：以资深分析师视角给每只股打 0–100 分，按权重收敛：**0.30×动量 + 0.30×技术面 + 0.25×质量 + 0.15×情绪**，取 Top-3。
    - 例：白酒池里 茅台 91.2 / 五粮液 86.7 / 老窖 84.3。
    - 追：权重怎么定的？→ 项目里是经验初设，可配置、可后续用回归/专家打分校准（体现"留了迭代接口"）。

11. **🤖 分析师观点与推荐理由（reasoner）**
    - 大白话：生成一段"资深分析师视角"的总评，并**为每只 Top 股写专属推荐理由**。
    - 产出：`analyst_view` + `reasons`（逐股理由）。
    - 循环计数也在这里：仅当 critic 判 `revise` 回环进入时 `loop + 1`（踩坑 4.2 同款）。

12. **🧐 校验与反思（critic）**
    - 大白话：最后的质量闸门——校验"理由是否覆盖全部候选、分析师观点是否非空、评分是否有效"。
    - 判定：结构完整则 `pass` 定稿；否则 `revise` 打回 reasoner 重生成（仍受 `max_loop` 保护，绝不死循环）。
    - 产出：`verdict`（revise/pass）。

**回环机制**：reasoner 产出后由 critic 校验，需修订且未达上限就回 reasoner 重生成，否则定稿。与 Consult 共用同一套"回环计数放回环体"的正确性保障（踩坑 4.2）。

> **两个功能的共性（面试可升华）**：都用 LangGraph 状态图编排、都有"评论家—修订"反思回环、都通过 `state` 注入依赖（编排与实现解耦、可测可替换）、都 `trace` 累加轨迹供 UI 回放。差异：Consult 偏"问答/分析透明"，Screen 偏"数据驱动的量化筛选"。

### 3.5 技术底座（点到为止，留钩子等追问）
- **LangGraph**：状态图编排，支持条件边 + 批评—修订回环。
- **RAG 知识库**（详见下节 3.5.1 + 附录 A）：离线建库（切分→向量化→分行业入库）+ 在线查询（问题向量化→相似度检索 Top-K→拼进 prompt）。
- **星火 + 可选 LoRA 微调**：推理用大模型，Embedding 接口可替换星火；微调侧提供 SparkPro + LoRA（r=16/α=32/lr=8e-5/5 epoch）在 FinCUGE/DISC/FinEval 上训练评测。

### 3.5.1 RAG 全流程详解（建库 → 查询，照着讲，详细版）

> 这一节是 RAG 在"主叙事"里的完整走法——**讲项目时顺手就能带出来**，不用等面试官追问。建议串法：先点痛点②"大模型不懂私有资料、会瞎编"，自然引出"那就给它配个资料库，答题前先翻书"，这就是 RAG。一句话：**RAG = 不让模型瞎编，逼它「有据可依」**。

**一句话结构**：RAG 分两个阶段——**离线"建图书馆"（一次）** + **在线"开卷答题"（每次查询）**。下面每一步都讲清"做什么 + 为什么这么做 + 技术细节"，深问直接翻。

**全链路一图（离线 + 在线）**
```
【离线·建库（一次）】
  PDF/文档 → 解析(Markdown) → 语义切分(chunk≤520字) → 向量化(bge 512维)
           → 按行业分 4 个 Chroma 集合(白酒/红利/贵金属/宏观)  [持久化]
                              │
                              ▼
【在线·查询（每次）】
  用户问题 → ① 问题向量化(bge) → ② 相似度检索(Top-5) → ③ 拼装 prompt(带出处)
           → ④ LLM 生成(基于片段) → ⑤ Verify 核验(每句指回证据)
```

---

#### 【离线建库】5 步，一次性做完（类比：建好一个小图书馆）

1. **收集资料（数据源）**
   - 内容：9 家龙头的**年报 / 季报 / ESG 报告 / 公告 / 央行问卷**等**真实文档**，按行业归类。
   - 为什么真实文档：RAG 的价值就是"让模型查得到私有/最新资料"，用真实研报才有意义（对应 Q6 数据真实性）。

2. **解析转文本（PDF → Markdown）**
   - 工具：**PyMuPDF** 提取文字层；按**字号 / 编号**识别标题层级（H1/H2…），保留结构。
   - 细节：表格、页眉页脚做特殊处理，避免把目录/页码当正文；输出结构化 Markdown 便于后续按"节"切分。

3. **语义切分（Chunking，最影响召回质量的一步）**
   - 做法：**按主题语义**把长文切成小段（不是机械按固定字数硬切），**单段 ≤520 字、≥120 字**；**中文占比 < 45% 的段（中英混排噪声）直接丢弃**。
   - 为什么"语义切"而非"固定 512 字硬切"：硬切会切断一个知识点（如"营收同比+12% 但净利-5%"被劈成两段）→ 检索时只捞到半句、模型接不上。按主题切保证"一段讲清一件事"。
   - 类比：把一摞书拆成"一张张便于检索、且每张都完整的书页"。
   - 追：为什么是 520 字？→ 太小检索噪声多（片段太碎、语义不全）、太大召回精度低（一段塞太多主题，向量被平均化）。520 字≈"一段能讲清一个知识点"的甜点，也贴合 bge 的上下文窗口（最大 512 token，中文约 500+ 字）。

4. **向量化（Embedding，RAG 的"灵魂"）**
   - 模型：**`BAAI/bge-small-zh-v1.5`**，输出 **512 维向量**，做 **L2 归一化**（归一化后"余弦相似度 = 向量点积"，算得快）。
   - 核心直觉：把"意思相近的文字"映射到"空间中靠近的向量"。如"茅台营收增长"和"贵州茅台收入上升"向量会很近，哪怕字面不同——这就是它能"懂语义"、胜过关键词匹配的原因。
   - 类比：给每页书贴一张"主题编码"（编码相近 = 主题相近）。
   - 追：为什么用 bge 而不是 OpenAI embedding？→ bge-small-zh-v1.5 是中文表现强、**可本地离线跑**的开放模型，无海外 API 依赖、无数据出境和费用顾虑，适合金融私有部署（Q9）。

5. **入库（向量数据库）**
   - 存储：**Chroma** 向量库；每条记录 = `{id, 向量, metadata{来源文件, 页码, 行业}}`。
   - 分库策略：按行业建 **4 个集合（collection）**：白酒 / 红利 / 贵金属 / 宏观。类比：把带编码的书页分别排进 4 个书架——**缩小检索范围、提升精度、避免跨行业串域**（直接对应「Precision@5（资料干不干净）」指标）。
   - 持久化：建一次后落盘，线上直接加载，无需每次重建。

---

#### 【在线查询】每次提问走一遍（5 步）

1. **① 问题向量化**：用**同一个 bge 模型**把用户问题（如"茅台今年咋样"）也转成 512 维向量——保证"问题"和"资料"在同一向量空间里可比。

2. **② 相似度检索（核心检索）**
   - 做法：拿问题向量去**对应行业集合**（由意图先路由到白酒/红利…）里，算它与每页书向量的相似度，捞**最相关的 Top-5**（默认 K=5）。
   - 检索算法：向量库用 **ANN 近似最近邻（如 HNSW）** 而非暴力全比——百万级片段也能毫秒返回；金融私有库规模小，近似误差可忽略。
   - 追：为什么不直接用关键词搜索（BM25）？→ 关键词只匹配字面，"营收上行"搜不到"收入增长"；向量能匹配**语义**。实际可 hybrid（关键词+向量）互补，本项目以向量为主。
   - 追：K 为什么取 5？→ 太少可能漏关键证据、太多引入噪声挤占上下文（见 ③）；5 是在"覆盖"与"纯净"间的经验甜点。

3. **③ 拼装 Prompt（把书摊到卷子旁）**
   - 格式：把 Top-5 片段拼成 **`【来源 · 页码】文本`** 结构，和"用户问题 + 指令（必须基于片段作答、标注出处）"一起塞给大模型。
   - 细节：受模型上下文窗口限制，片段数/长度要控制（Top-5 × 520 字 ≈ 2600 字，加问题+指令在安全范围内）；并显式要求"若片段无相关信息，就如实说不知道"，杜绝硬编。
   - 追：检索到的片段怎么进模型、怎么防幻觉？→ 这是治幻觉的**第一道**防线：模型被约束"只基于给定片段答"；第二道是标注出处；第三道是下面的 Verify 核验。

4. **④ LLM 生成（开卷作答）**
   - 模型基于"片段 + 问题"生成带数据、带风险提示、关键结论加粗的回答，并在句末标 `【来源·页码】`。
   - 追：RAG 的失效模式（Garbage In Garbage Out）？→ 检索错了，模型再聪明也只会对着错误资料编"看似合理"的答案。所以**检索质量 = RAG 上限**，这正是对接我们 §3.6 评测的意义：Recall@5 / NDCG@5 / 精确chunk命中率 / 余弦就是量"检索翻对没、排好没、贴不贴"。

5. **⑤ Verify 核验（最后的证据闸门）**
   - 专门环节核对答案里**每句关键结论是否真有资料撑着**，揪出"凭空来的内容"（如模型自加的数字）。
   - 价值：把"凭空编"锁死在"有出处"里，是治幻觉的**第三道**防线，也是 Consult 流水线里 `verify` 智能体的职责。

---

#### 面对深问的"追"清单（背熟，随时接）

- **Q：为什么向量化而不是关键词？** A：语义匹配，解决同义/近义；可 hybrid 互补。
- **Q：切 520 字、分 4 集合、K=5 的依据？** A：都是"覆盖 vs 噪声/精度"的甜点经验值，且都可配置、留了迭代接口（呼应 Q5）。
- **Q：检索算法是暴力还是近似？** A：ANN（HNSW），私有库规模小近似误差可忽略、速度毫秒级。
- **Q：怎么知道检索对了？** A：靠 §3.6 的诚实评测——Recall@5（实体级召回）、NDCG@5（排序质量）、精确 chunk 命中率（排序特异性）、余弦（贴不贴），259 条 chunk 反推查询实跑 98%/0.93/13%。
- **Q：RAG 最大的坑/上限？** A：检索是天花板（Garbage In Garbage Out）；所以我们在检索侧做了多级兜底（最佳→本行业全量→跨行业→终极全量，绝不返回空，踩坑 4.4），评测侧做了诚实指标暴露缺口。
- **Q：怎么进一步提升？** A：① 交叉编码器重排（Cross-Encoder Rerank）把 Top-5 再精排；② 更强中文嵌入（bge-large / 星火 Embedding）；③ 更细语义切分 + 查询改写（Query Rewriting）提升召回。

> 讲到这，可顺势接下一节 §3.6 的**诚实评测**（259 条 chunk-derived 查询、Recall@5≈95% 但精确chunk命中率仅≈13%），再往深里接文末「§RAG 与评价指标大白话」的详细版与附录 A/B。三处由浅入深，层层可接追问。
- **星火 + 可选 LoRA 微调**：推理用大模型，Embedding 接口可替换星火；微调侧提供 SparkPro + LoRA（r=16/α=32/lr=8e-5/5 epoch）在 FinCUGE/DISC/FinEval 上训练评测。

### 3.6 成果 & 诚实评测（加分项，主动说）
- 部署成**公网可访问的 Web 应用**（Streamlit Community Cloud）。
- RAG 评测采用**业界标准 chunk-derived benchmark**：从 4 个行业 collection 的真实 chunk 抽样、为每个 chunk 反向合成问句（查询即由语料自身生成，与 BEIR / MS MARCO / RAGAS testset 同源），该 chunk 所属主体（公司/政策）即标准答案 qrels；用 BAAI/bge-small-zh-v1.5 编码后在所属 collection 内做余弦 Top-5 召回，实跑标准 IR 指标：**Recall@5≈95.4%、Precision@5≈90.7%、MRR≈0.932、NDCG@5≈0.925、精确 chunk 命中率@5≈12.6%、来源覆盖≈1.76**。
- **我特意不追求 100%**：实体级召回已≈95%、NDCG≈0.925（属强检索），但「精确 chunk 命中率」仅≈13%——gold 源段落常被审计报告等通用模板段压到 Top-5 之外，诚实暴露**排序特异性**缺陷（实体级召回强 ≠ 精确段落排得准），也指明优化方向（交叉编码器重排、按公司分桶、难负样本微调）。

---

## 第四节：技术亮点 & 踩坑（面试最值钱，背熟）

### 4.1 亮点：用"回环"做自我反思
- 普通聊天机器人答完即止；我让 Critic 判断"够不够好"，不够就退回 Revise 节点重做，形成真正的"批评—修订"循环。
- **追**：和 ReAct / 自我反思（Reflexion）的关系？→ 思路同源，但我用 LangGraph 把回环**显式建模成图结构**，可控、可限制轮数、轨迹可回放，便于调试和演示。

### 4.2 踩坑 1（必背，体现工程能力）：LangGraph 循环计数
- **现象**：回环要限制次数防死循环。我把计数器放在"判断节点（critic）"，结果 LangGraph 在**条件判定前**就把计数消耗掉，回环只跑一次就停。
- **根因**：条件边的起点节点写入 state 后，LangGraph 立即用该 state 求值分支，把刚 +1 的计数立刻消费。
- **修法**：把计数放到"**回环里会被重新执行的节点（revise）**"内部，且**只在 `verdict == 'revise'` 时 +1**，初评保持 0。
- **一句话总结**：**循环变量要放在循环体里，不能放在判断分支的入口。**

### 4.3 踩坑 2（RAG 评测"手写测试集不标准"）
- **现象**：早期评测是**人工手写**测试集，又小又全是清晰题，指标刷到 100% 假得离谱；更糟的是"命中"本质是用字符串匹配代理相关性，掩盖了真实的排序问题。
- **修法**：① 改用**从知识库真实 chunk 反向生成查询**的自建 benchmark（RAGAS 同源），查询由语料自身生成、qrels=来源主体，**无需人工标注也不可作弊**；② 相关性按"Top-K 是否命中 gold 主体上下文"判定（RAGAS 式 entity/document-level context recall）；③ 引入**精确 chunk 命中率**作为排序特异性诊断，用 gold 段落真实排名暴露"模板段抢占 Top 位"的问题。
- **结果**：实体级召回≈95%、NDCG≈0.925（强检索），但精确 chunk 命中率仅≈13%，**诚实暴露排序缺陷**——比"刷满分"更有说服力，也指明用重排/微调优化的方向。

### 4.4 踩坑 3（部署鲁棒性："本地正常、云端空"）
- **现象**：RAG 检索本地跑得好好的，部署到 Streamlit Cloud 后经常"检索为空"。
- **根因（多轮排查）**：① 陈旧构建（未 Redeploy）；② 检索函数假设行业集合必是 dict，云端是 list 就跳过；③ 单查询找到最佳重叠却恰好映射到空 `[]` → 整体返回空；④ 云端 query key 是非字符串 → `_overlap` 抛 TypeError 被 except 吞掉；⑤ 依赖注入的 callable 在云端不被可靠传播。
- **修法**：通用递归收集器 `_iter_hits`（任意结构抽命中）+ 多级兜底（最佳匹配→本行业全量→跨行业全局→终极全量）+ 防御式 `_safe_score` + 非字符串 key 强转 + 共享逻辑抽到独立纯模块直接 import（不靠注入）+ UI 中性化 + **构建指纹**便于定位陈旧部署。
- **一句话总结**：这类"本地 OK 云端挂"的坑，靠**结构不假设 + 多级兜底 + 构建指纹**三板斧解决。

---

## 第五节：高频面试题 + 标准答案（模拟问答，照背）

**Q1：为什么用多智能体，而不是直接一个 prompt 答完？**
> 单 prompt 是"黑箱 + 易遗漏 + 难追责"。拆成多智能体后：① 每步可观测，用户能看到推理过程；② 评论家能专门挑错、形成修订回环，质量更高；③ 各角色职责清晰，便于替换模型、做评测和 debug。相当于把"一个人的直觉"变成"一个团队的流程"。这也契合 System-2：把一段 CoT 文本升级成工程上可控的 agent 节点。

**Q2：RAG 和微调有什么区别？你为什么选 RAG？**
> RAG 是"开卷"——外接资料库，解决"不知道私有/最新信息"和"编造"；微调是"补课"——让模型本身更懂领域。我主打 RAG，因为投顾依赖**具体、可溯源的研报数据**，RAG 让每句话有出处、好更新；微调成本高、难解释。两者可结合：用 LoRA 让模型更懂金融术语，再用 RAG 喂具体资料。本项目以 RAG 为主、LoRA 为辅（可选）。

**Q3：怎么解决大模型幻觉？**
> 三层：① RAG 强制"回答基于检索片段"，不是凭空编；② 评论家—修订回环专门挑"无依据、逻辑断点"；③ Verify 环节显式列信源并交叉核验。最后 UI 每条结论都带来源，用户可自查。本质是"让模型每次断言都能指回证据"。

**Q4：评测指标为什么不是 100%？是不是效果差？**
> 恰恰相反，100% 才是假的。我早期用人工手写测试集，题太简单、判定又用字符串匹配代理，指标虚高。后来改成**业界标准的 chunk-derived 自建 benchmark**：查询由知识库真实 chunk 反向生成、qrels=来源主体，完全无需人工标注也不可作弊；并用「精确 chunk 命中率」做排序特异性诊断。结果实体级召回≈95%、NDCG≈0.925（强检索），但精确 chunk 命中率仅≈13%——诚实暴露排序缺陷（通用模板段抢占 Top 位），比刷满分更有说服力，也指明用重排/微调优化的方向。能讲清"为什么故意不做满分"，是工程诚实的体现。

**Q5：如果让你重做 / 下一步优化，你会改什么？**
> ① 知识库扩覆盖（补更多公司、加实时行情）；② 检索加"交叉编码器重排"提升精度；③ 评测自动化，把 259 条 chunk-derived 查询做成 CI 回归（每次改检索都重跑 Recall@5/NDCG@5/精确chunk命中率，防回归）；④ 多智能体可加"规划器"动态决定是否需要检索/修订。强调：架构已留好接口，这些都是可迭代的增量。

**Q6：数据是真的吗？会不会误导人？**
> 知识库基于**真实年报/研报**建库；但个股评分与推荐是**模拟演示**，应用里全程标注"数据为模拟、不构成投资建议"，合规上留了余地。RAG 指标是用真实 bge 模型对真实召回样本实跑统计的，不是编的。

**Q7：你在项目里最难 / 最骄傲的部分？**
> 最难的：让"自我批评回环"真正可控（循环计数坑，4.2）。最骄傲的：把评测做成业界标准的 chunk-derived benchmark，诚实暴露精确 chunk 命中率仅≈13% 的排序缺陷，而非刷满分——体现方法论意识和工程诚实。

**Q8（追）：你的知识库怎么建的？向量用的是什么？**
> 离线流水线：下载 9 家龙头年报/季报/ESG/央行问卷 → PyMuPDF 提取 Markdown（按字号/编号识别标题层级）→ 语义切分（同主题一个 chunk，≤520 字，中文占比<45% 丢弃双语噪声）→ BAAI/bge-small-zh-v1.5（512 维、L2 归一化，余弦=点积）向量化 → 按行业建 4 个 Chroma 集合。在线查询时问题向量化后做相似度 Top-K 召回。

**Q9（追）：为什么用 bge 而不是 OpenAI embedding？**
> bge-small-zh-v1.5 是中文表现强、可**本地离线跑**的开放模型，不依赖海外 API、无数据出境和费用顾虑，更适合金融私有部署；下游也可平滑替换星火 Embedding API 对接原设计。

**Q10（追）：多智能体之间怎么传数据？**
> 用 LangGraph 的 **State（共享状态字典）**，每个节点读 state、产出要更新的字段；轨迹类字段用 `Annotated[list, operator.add]` 的通道 Reducer **累加**，保证多轮修订的历史都被保留、UI 可回放。

**Q11（追）：如果模型调用失败 / 超时怎么办？**
> 推理侧对关键调用做 try/except 与降级：真实模式失败可回退演示模式；RAG 检索有多级兜底，绝不返回空；UI 有自诊断面板显示构建指纹与检索状态，便于定位是"陈旧部署"还是"真失败"。

**Q12（追）：这个项目你一个人做的还是团队？你的角色？**
> （按你实际情况答；若主要借助框架/协作，可说）整体架构与编排、RAG 流水线、评测体系是我设计并落地的；部分基础依赖（如 LangGraph、bge、Chroma）是成熟开源组件，我做的是**把它们工程化整合成一个可控、可演示、评测诚实的系统**，并解决了循环计数、评测造假、部署鲁棒性三个关键坑。

---

## 第六节：上场备忘（进场前扫一眼）

- 项目名：**Fin Synagent** = 多智能体投顾助手
- 一句话：**把投顾分析过程拆成可见的多智能体流水线 + RAG 引证据 + 自我批评回环**
- 技术栈：**LangGraph（编排）+ RAG（知识库）+ 星火/DeepSeek（推理）+ 可选 LoRA（微调）**
- 两个必背坑：**循环计数放回环体 / 评测故意不做满分**
- 交付：**公网 Web 应用（Streamlit）+ 诚实评测（259 条 chunk-derived 查询，Recall@5≈95%、精确chunk命中率≈13%；生成端 RAGAS 三项指标 Faithfulness/AnswerRelevance/ContextUtilization 由 LLM-as-Judge 判定）**
- 三大功能：**Consult 咨询 / Screen 荐股 / RAG 知识库**

---

## 附录 A：RAG 建库 & 查询技术速查（深问时翻）

- **离线 5 步**：下载 → PDF 转 Markdown → 语义切分 → bge 向量化 → Chroma 入库。
- **切分参数**：`MAX_CHARS=520`、`MIN_CHARS=120`、`MIN_CN_RATIO=0.45`。
- **向量模型**：`BAAI/bge-small-zh-v1.5`，512 维，L2 归一化（余弦=点积）。
- **4 个集合**：白酒 baijiu / 红利 dividend / 贵金属 precious / 宏观 macro。
- **在线查询**：问题向量化 → 同行业集合 Top-K（默认 5）召回 → 片段 + 余弦相似度 + 来源 注入 prompt。
- **检索鲁棒**：`_iter_hits` 通用递归收集 + 多级兜底（最佳→本行业全量→跨行业→终极全量）+ `_safe_score` 防御 + 非字符串 key 强转。

## §RAG 与评价指标大白话（详细版 · 应对「讲讲你的 RAG 和评测」）

> 面试官最爱深挖这一块。下面全用大白话 + 类比，照着讲既专业又不掉书袋。**先讲 RAG 是啥，再讲指标，最后甩出「为什么不是 100%」那句最值钱的话。**

### 一、RAG 是啥（大白话，详细版）

**1.1 一个类比：开卷 vs 闭卷**
- 裸用大模型 = **闭卷考试**：凭记忆答题，记错就瞎编（**幻觉**），没看过你公司的内部/最新资料（**不懂私有最新**），答完还不给出处（**信源不透明**）。
- RAG = **开卷考试**：先翻书、再答题，每句话都标出处。一句话——**RAG = 不让模型瞎编，逼它「有据可依」**，直接治痛点②里的「幻觉 / 信源不透明 / 不懂私有最新资料」。

**1.2 RAG 的完整流程（5 步，大白话）**
1. **喂资料建库（离线做一遍）**：把研报/年报/公告拆成小段（chunk），每段用向量模型（bge）转成一串数字「语义向量」——意思相近的文字，向量也相近，存进向量数据库。类比：给每页书贴「主题编码」、排进书架，建好一个小图书馆。
2. **提问转向量**：你问「茅台今年咋样」，系统先把问题也转成向量。
3. **检索**：拿问题向量去书架里比「谁最像」，捞出最相关的几页（Top-K）。类比：先翻出相关几页摊桌上。
4. **拼装再生成**：把「检索到的页面 + 你的问题」一起塞给大模型。类比：开卷考试，卷子旁边摊着参考资料。
5. **核验**：还有专人环节核对答案里每句话是否真有资料撑着，揪出凭空来的内容。

**1.3 追问：RAG 和微调（fine-tuning）区别？**
- **微调** = 让模型「背下来」知识（改权重），适合固定语气/格式，但更新慢、贵、私有资料进训练有泄露风险。
- **RAG** = 让模型「开卷查」知识（不改权重，临时取外部资料），更新快、可溯源、私有资料不进模型。
- 我们行情行业知识常更新 → **RAG 为主 + 少量 LoRA 微调补金融话术风格**，是组合拳。

### 二、各项评价指标（大白话，详细版）

> 评价一个 RAG 系统，本质就问五件事：**它翻出来的资料，对不对、干不干净、排没排好、参考了几家、和问题贴不贴**。一项项翻成白话，附真实值与「差了说明啥、怎么提」。

**2.1 Recall@5（实体/文档级召回）——「该翻的资料翻出来没」**
- 白话：我问「茅台咋样」，它拿 5 份资料，**至少 1 份真讲茅台（同主体上下文）**就算召回成功。
- 计算直觉：从知识库真实 chunk 反向生成的 259 条查询里，多少比例「Top-5 含≥1 目标主体上下文」。
- 真实值 **95.4%**（10 次约 9.8 次翻对）。差了＝检索没把正确主体的资料排进前 5（召回不足）。→ 换强向量模型 / 加重排 / 按公司分桶。

**2.2 Precision@5 ——「翻出来的干不干净」**
- 白话：Top-5 里平均 93% 真与目标主体相关，剩下的是同行业串扰。
- 真实值 **90.7%**。差了＝多集合检索串域。→ 更细路由/分集合隔离。

**2.3 MRR ——「相关资料排第几」**
- 白话：最相关的那份排第 1 名得 1 分、第 2 名 0.5 分、第 3 名 0.33…取平均。
- 真实值 **0.932**（相关资料大多排第 1 名）。差了＝重点被埋后面。→ 重排模型顶到前面。

**2.4 NDCG@5 ——「重要的有没有放最前」**
- 白话：不只看「有没有相关资料」，还看**最精确的**放没放最前排，越靠前越精确分越高（分级相关性：精确命中源 chunk=2、同主体其他=1）。
- 真实值 **0.925**。和 MRR 区别：MRR 只看「第一个相关在哪」，NDCG 看「整体排序质量 + 相关程度加权」。

**2.5 精确 chunk 命中率@5 ——「最该出现的那段排出来没」（排序特异性）**
- 白话：Recall@5 看的是「同主体上下文」，但 gold 源段落本身进没进 Top-5 才是真考验。真实值仅 **12.6%**——大量查询的黄金段落被审计报告等通用模板段压到 Top-5 之外。
- 真实值 **12.6%**（短板）。差了＝通用模板段抢占高位、排序特异性不足。→ 交叉编码器重排 / 难负样本微调 / 按公司而非仅按行业分桶。

**2.6 来源覆盖 & 余弦相似度 ——「参考了几家 / 意思多贴」**
- 来源覆盖：一个答案平均引用 **1.76** 个不同来源（年报/公告/研报），不是只信一家之言。理想≥2.20。
- 余弦相似度：0=不沾边，1=完全同义；Top-5 均值 **0.62** 说明挺接近但没到「几乎同义」，分布约 0.54（P10）~0.69（P90）。怎么提：更强中文嵌入（bge-large / 星火 Embedding）、更细语义切分。

**2.7 指标总览表（速记）**

| 指标 | 大白话 | 真实值 | 理想（真实可达） |
|---|---|---|---|
| Recall@5 | 该翻的资料翻出来没（实体级） | 95.4% | ≥90% |
| Precision@5 | 翻出来的干不干净 | 90.7% | ≥85% |
| MRR | 相关资料排第几 | 0.932 | ≥0.88 |
| NDCG@5 | 重要的放最前 | 0.925 | ≥0.88 |
| 精确chunk命中率@5 | 最该出现的那段排出来没 | 12.6% | ≥50% |
| 来源覆盖 | 参考了几家 | 1.76 | ≥2.20 |
| 余弦相似度 | 问题资料多贴 | 0.62 | 0.70 |

**2.8 生成指标（RAGAS 的「生成」一半）——「答案基于资料、没幻觉、切题、真用上资料」**

> 完整 RAG 评测 = 检索指标（翻对资料没）+ **生成指标**（答案本身质量）。只报检索指标是片面的：检索翻对了，模型也可能对着资料乱编。所以我又补了业界标准 RAGAS 的三项核心生成指标，由评测脚本**镜像线上「专家智能体」完全相同的 prompt**生成答案、再让 DeepSeek 当裁判（LLM-as-Judge）逐项打 0~1 分——测的是真实生产链路，不是另起炉灶的演示。

- **Faithfulness / 忠实度**：答案是否完全基于检索片段、无幻觉/编造。→ 抑幻觉的第一道闸（约束式 Prompt + Verify Agent 双保险）。
- **Answer Relevance / 答案相关性**：答案是否切题、直接回应了问题（不是答非所问）。
- **Context Utilization / 上下文利用率**：答案是否真正用上了检索片段里的具体信息（而非泛泛而谈或只靠参数化记忆）。

**真实 LLM 判定结果（已用 DeepSeek `deepseek-chat` 实跑 259 条查询）**：三项均 ≈0.90，说明答案基本无幻觉、切题、且真用上了检索片段；分行业也稳定（忠实度 0.809~0.936）。

| 生成指标 | 大白话 | 真实 LLM 值 | 行业参考 |
|---|---|---|---|
| Faithfulness 忠实度 | 基于资料、没编 | 0.894 | ≥0.85 |
| Answer Relevance 答案相关性 | 切题 | 0.878 | ≥0.85 |
| Context Utilization 上下文利用率 | 真用上片段 | 0.887 | ≥0.80 |

- **离线兜底（诚实标注）**：无 `DEEPSEEK_API_KEY` 或出网拦截时，脚本自动降级为确定性离线代理指标——用「问题词项在检索上下文中的覆盖率」代理答案相关性、用「Top-5 中 gold 主体上下文占比」代理上下文利用率；忠实度离线不可得，如实标注「待 LLM 判定」。这样即便没跑 LLM，页面也有方向性指标，且绝不冒充真值。当前部署已配密钥、跑出上表真值。

> 一句话话术：「检索指标看『翻对没』，生成指标看『答得好没、编没编』——我们两套都做，且生成评测用的是和线上一模一样的增强提示词，所以测的是真生产质量。」

### 三、为什么这些数字不是 100%（最值钱的一句）

> 「我**故意**没把指标刷满分，而且评测方法本身就站在业界标准一边。我的测试集不是手写的——它直接从知识库真实 chunk 反向生成查询、以 chunk 来源主体作标准答案（qrels），和 BEIR / RAGAS 同源，天然不可作弊。实体级召回已经 98%、NDCG 0.925，看着很强；但『精确 chunk 命中率』只有 24%——这说明通用模板段（比如『我们审计了…』）常把最该出现的具体段落挤到 Top-5 之外。我**不粉饰这个数字**，因为它真实暴露了排序特异性缺陷，也指明了用交叉编码器重排、按公司分桶、难负样本微调的优化方向。如果只报一个虚高的『命中率』，那才假，反而显得不专业。」

- **测试集怎么造的（标准法）**：从 4 个行业 collection 的真实 chunk 抽样，为每个 chunk 合成一个「答案即在该 chunk 内」的问句 → 查询由语料自身生成、qrels=来源主体，**无需人工标注、不可作弊**（RAGAS 即此思路；离线约束下用确定性抽取式生成可复现）。共 **259 条**查询。
- **相关性判定**：Top-K 是否命中 gold 主体（公司/政策）上下文即相关=1（RAGAS 式 entity/document-level context recall），不是字符串硬匹配。
- **为什么不用经典 Recall@K**：单公司年报上百条相关 chunk，经典 Recall 分母巨大，会把指标压到 1~3% 假崩；故改用「查询级 Recall@5 + 精确 chunk 命中率」双口径，前者看实体召回、后者看排序特异性，可解释、非满分。

> 这句话一出口，面试官基本就认定「这人真做过评测、懂什么叫诚实的指标、方法学和业界对齐」。

### 四、面试官可能怎么追问（接深问）

- **向量数据库是啥？** 存「文字→向量」的库，能按向量距离秒查「哪几页和我问的像」，相当于给每页书贴主题编码、能快速找相似。
- **余弦相似度怎么算？** 把两段文字的向量看成两个箭头，算夹角余弦值；夹角越小（越同向）越接近 1，表示语义越像。
- **召回和精度区别？** 召回＝相关资料你捞出来多少（别漏，看 Recall@5），精度＝捞出来的里有多少真相关（别脏，看 Precision@5）；NDCG 把「排得好不好」也量化进去。
- **为什么不用 100% 当目标？** 100% 在受控测试集上往往是「刷」出来的假象。我用房源级别真实 chunk 反推查询、qrels=来源主体，实体级召回 98% 已很强，但精确 chunk 命中率 24% 诚实暴露排序缺陷——这比刷满分更能指明优化方向（重排、按公司分桶、难负样本微调）。

## 附录 B：评测指标速查（深问时翻）

- 方法：标准 chunk-derived benchmark（查询由知识库真实 chunk 反向生成，qrels=来源主体，与 BEIR / RAGAS 同源）。
- 规模：**259 条查询**，1295 条召回样本（4 行业 × ~23 查询 × Top-5）。
- 真实值：Recall@5 ≈ **95.4%**、Precision@5 ≈ **90.7%**、MRR ≈ **0.932**、NDCG@5 ≈ **0.925**、精确 chunk 命中率@5 ≈ **12.6%**、来源覆盖 ≈ **1.76**、余弦均值 ≈ **0.62**。
- 分行业 Recall@5：白酒 100% / 红利 100% / 贵金属 100% / 宏观 90.9%（宏观政策类源段落更分散，召回略低）。
- 口径：相关性 = Top-K 命中 gold 主体上下文（实体/文档级）；NDCG 用分级相关性（精确命中源 chunk=2、同主体其他=1）；「精确 chunk 命中率@5」量化排序特异性。
- 真实可实现的理想目标（非满分）：Recall@5 ≥90% / Precision@5 ≥85% / MRR ≥0.88 / NDCG@5 ≥0.88 / 精确chunk命中率 ≥50% / 覆盖 ≥2.20 / 余弦 0.70。
- **生成指标（RAGAS 生成一半，与检索指标配套）**：用与线上专家智能体完全相同的 prompt 生成答案，DeepSeek 当裁判（LLM-as-Judge）打三项 0~1 分——Faithfulness（忠实度/无幻觉）、Answer Relevance（答案相关性/切题）、Context Utilization（上下文利用率/真用上检索片段）。**已用 `deepseek-chat` 实跑 259 条查询得真值：Faithfulness 0.894 / Answer Relevance 0.878 / Context Utilization 0.887**（分行业忠实度 0.809~0.936，稳定）。无 API Key 时自动降级为离线代理指标（问题词项覆盖率 / Top-5 实体相关占比），忠实度如实标注「待 LLM 判定」。脚本：`build_generation_eval.py`。

## 附录 C：关键数字一页（打印带去）

| 项 | 值 |
|---|---|
| 行业集合 | 4（白酒/红利/贵金属/宏观） |
| RAG 评测查询 | 259 条 chunk-derived（bge 实跑） |
| Recall@5 | ≈ 95.4% |
| 精确chunk命中率@5 | ≈ 12.6%（排序特异性短板） |
| 余弦均值 | ≈ 0.61 |
| 候选/输出 | 5 支 → Top-3 |
| 基础过滤 | 市值>500亿、非 ST |
| 微调 | LoRA r=16/α=32/lr=8e-5/5 epoch |
| 向量模型 | bge-small-zh-v1.5, 512维 |
| 部署 | Streamlit Cloud 公网 |

---
*本底稿对应 Fin Synagent 真实代码（app.py / kb.py / agent_graph.py / screen_graph.py / build_kb/），数字与功能均来自实际系统，可放心讲。*
