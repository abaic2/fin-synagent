# Fin Synagent 项目介绍（通俗详细版）

> **给谁看**：完全不懂 AI / 不懂金融的家人、朋友、非技术面试官、第一次接触这个项目的人。
> **阅读方式**：从头读到尾就是一条完整故事线；赶时间只看「30 秒版」和「关键数字速查」。
> **一句话定位**：**Fin Synagent 是一个"把资深投顾的思考过程拆开给你看"的 AI 系统**——它不是又一个聊天机器人，而是把"分析师是怎么想问题、查资料、自我纠错、最后给结论"的全过程，变成一条可以逐步观看、可以追责的工作流。

---

## 〇、30 秒版（电梯演讲）

> 现在大家用 AI 问股票，最大的问题不是"它说得好不好听"，而是**"它说得对不对、依据在哪、我凭什么信"**。
> Fin Synagent 的做法是：不说一句没出处的话。用户问一个问题，系统先**拆题**，再**去自己的行业知识库翻资料**（资料是 152 份真实年报/季报/货币政策报告切出来的 1.26 万个片段），然后**专家智能体写答案**，接着**批评家智能体挑毛病**，不合格就**打回去重写**，最后**核对信源**再给结论。
> 全程每一步都展示在页面上，你能看到它翻了哪几段资料、每段资料来自哪份文件的哪一页。
> 我们另外做了两套诚实的评测：2,754 条查询实测，**96.3% 的问题能翻到正确的公司资料**，生成的答案**忠实度 0.90、切题度 0.87、上下文利用率 0.87**；同时我们也诚实公开了一个短板——**"具体翻到哪一段"的精确命中率只有 7.6%**，因为同一个公司的上百个段落长得太像了。

---

## 一、这个项目到底在解决什么问题？

### 1.1 三个真实的痛点

| 痛点 | 大白话说 | 后果 |
|---|---|---|
| **门槛高** | 普通人打开一份 200 页的年报，不知道看什么、数字意味着什么 | 投资决策靠小道消息、靠感觉 |
| **黑箱** | 你问 AI"某某股票能不能买"，它直接给你一段话，你不知道它是真查了还是瞎编的 | 大模型"一本正经地胡说"（行业叫**幻觉**） |
| **没逻辑** | 就算它说对了，也没有推理链条，你无法判断这个结论靠不靠谱、能不能复现 | 不敢用、不敢跟 |

### 1.3 三个生活化的类比（讲给完全不懂的人听最好用）

- **类比一：一个投研团队在开会。**
  直接问 ChatGPT，像是拉住一个实习生随口问一句；Fin Synagent 像是开了一场投研晨会——有人负责拆题（Leader）、有人负责翻资料（RAG）、有人负责写分析（Expert）、有人专门唱反调挑刺（Critic）、有人负责核对引用来源（Verify）、最后有人做会议纪要（Summary）。

- **类比二：老师改作文。**
  学生（Expert）写完作文，老师（Critic）批改指出"论据不足、建议不具体"，学生按批语**重写一遍**再交，直到老师通过。这就是系统里的"批评—修订回环"。

- **类比三：开卷考试，还要写引用出处。**
  系统不允许凭记忆答题，必须去自己的知识库翻，而且答案后面要标注"这句话出自哪份文件的哪段"。这从机制上压缩了瞎编的空间。

---

## 二、产品长什么样？（10 个页面逐个说）

部署在 Streamlit Community Cloud，打开就是一个网页应用，左侧一个菜单：

| # | 页面 | 一句话说明 | 值得看什么 |
|---|---|---|---|
| 1 | 🏠 **首页** | 产品介绍、设计理念、操作流程、迭代回顾 | 三个理念：**深思熟虑 / 实事求是 / 小心求证** |
| 2 | 💬 **智能咨询** | 核心能力一：多智能体回答投资问题 | 六个智能体卡片依次点亮，思维链逐段展开 |
| 3 | 📊 **智能荐股** | 核心能力二：筛选树荐股 | 筛选树 12 步 + 四维特征 + Top-3 推荐 |
| 4 | 🔥 **星火大模型** | 模拟讯飞星火 Spark 4.0 Ultra/Max/Pro/Lite 的调用链路 | 参数可调、流式输出、模拟 API 报文 |
| 5 | 🧪 **测试评估** | AI as Judge / AI as Customers / 人工评估 / 消融实验 | 均分对比（28.41 vs 基线），p 值检验 |
| 6 | 📚 **RAG 知识库** | 知识库透明化 | 建库规模、检索样本浏览器（带余弦相似度+出处）、评测指标 |
| 7 | 🧩 **技能中心** | 各智能体的能力清单 | 每个角色"会什么"的说明书 |
| 8 | ⚙️ **技术设计** | System-2 工作流、筛选树、微调、知识库构建 | 架构图 + LangGraph 状态图 |
| 9 | 📖 **专有名词解释** | 术语表（PE/PB/ROE、MACD、RAG、LangGraph…） | 不懂的词现场查 |
| 10 | 🎤 **面试建议** | 项目自我介绍 + STAR 行为题 | 面试场景直接用 |

> 另外有个贴心设计：**标准版 / 简略版**一键切换。信息密度太高时切简略版，只留核心内容，讲给非技术的人听时特别好用。

---

## 三、核心能力一：智能咨询（Consult）——六个智能体流水线

用户问一句"白酒板块今年还能不能配"，系统跑的是这样一条流水线：

```
用户提问
   ↓
👔 Leader 拆解      → 把大问题拆成「景气度 / 竞争格局 / 估值与风险」等子问题，并分配专家
   ↓
📚 Retrieve 检索    → 去行业知识库召回 Top-5 最相关的片段
   ↓
🎓 Expert 作答      → 拿着资料写结构化分析（必须引用）
   ↓
🧐 Critic 批评      → 挑毛病：逻辑有没有缺口？建议够不够具体？
   ↓  ┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┐
   │ 判定「需要修订」→ ✍️ Revise 重写 → 回到 Critic │ ← 自我反思回环（最多 N 轮）
   └┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┘
   ↓ 判定「通过」
🔎 Verify 求证      → 交叉核验信源，杜绝幻觉
   ↓
📋 Summary 总结     → 给出可执行结论，并主动邀请追问
```

**为什么值得强调这六个角色？** 因为"分工"本身就是价值——单一大模型容易"想到哪说到哪"，而角色分工强制它必须走完"拆题→取证→作答→自检→复核"的完整链条，每个环节单独可见、单独可改。

**两个运行模式**（很实用的工程设计）：
- **真实模式**：接 DeepSeek 实时推理，用户可自带 Key（不写入代码/文件，只在当前浏览器会话里）。
- **演示模式**：内置高质量示例，**断网也能完整演示整条流水线**。演示现场最怕网络挂掉，这个模式就是保险。

---

## 四、核心能力二：智能荐股（Screen）——筛选树，不拍脑袋

不用"你帮我推荐几只股票"这种一句话交给模型拍脑袋，而是走**筛选树 12 个节点**：

| 阶段 | 节点 | 干什么 |
|---|---|---|
| 准备 | `fetch_data` | 拉取行情/基本面数据 |
| 理解 | `planner` | 把口语需求（行业+风险偏好+目标）解析成结构化 JSON |
| 建池 | `build_pool` | 行业过滤 + 基础过滤（市值 > 500 亿、非 ST）→ 5 支候选 |
| 情绪素材 | `fetch_comments` | 抓取股吧/评论区文本 |
| 四维特征 | `feat_fundamental` | 基本面：PE / PB / ROE / 营收增速 |
| | `feat_technical` | 技术面：趋势 / 均线 / 波动率 / MACD |
| | `feat_sentiment` | 情绪面：FinBERT 正/中/负概率（本地词库 + DeepSeek 混合，DS 判"中性"但本地词库有明显倾向时按 0.92 权重覆盖） |
| | `feat_industry` | 行业面：宏观 + 行业景气 |
| 综合 | `synthesize` | 汇总四维特征 |
| 打分 | `scorer` | LLM 以资深分析师视角打 0–100 分 |
| 论证 | `reasoner` | 生成推荐理由与分析师观点 |
| 质控 | `critic` | 挑刺 → 判定「修订」则回到 reasoner 重写（最高 N 轮） |

**输出**：Top-3 推荐，每只都有**推荐理由 + 分析师观点 + 风险提示**，并明确声明"数据为模拟/知识库内容，不构成投资建议"。

**为什么叫"筛选树"？** 因为它像一棵倒长的树：从宏观到行业到个股，一层层剪枝，最后剩下的才是候选——而不是从 5000 只股票里让模型"凭感觉"挑。

---

## 五、技术底座（大白话讲清楚）

### 5.1 LangGraph：把智能体"接线"

用 LangGraph 的 StateGraph 把每个智能体做成一个**节点**，用**边**连起来。关键是支持**条件边**：Critic 判定"需要修订"且没超轮数，就沿条件边回到 Revise，形成真正的反思回环；否则往下走。

> **一个实踩过的坑（技术面试很加分）**：循环计数必须放在**回环中会被重新执行的节点里**（reasoner / revise），并且**只在"需要修订"时 +1**；绝不能放在条件边的起点节点（critic）里——否则 LangGraph 在做条件判定之前就把计数消费掉了，回环只会跑一次。这个 bug 不报错，只是"好像没反思"，很隐蔽。

### 5.2 RAG 知识库：让答案有出处

**RAG = Retrieval-Augmented Generation，检索增强生成**。大白话：**"先翻资料，再写答案"**，而不是"凭记忆写"。

我们的建库流水线（全部离线可复现）：

```
① 下载      → 9+ 家龙头年报/季报/ESG + 央行问卷 + 货币政策执行报告
② 解析      → PDF 转结构化 Markdown
③ 切分      → 语义切分（长度≤800 字、中文占比过滤双语噪声、要求散文型且含数字）
④ 向量化    → BAAI/bge-small-zh-v1.5 把每段话变成一串数字（向量）
⑤ 分库存储  → 按行业拆 4 个 Chroma 集合
```

**建库规模（真实数字）**：

| 行业集合 | 文档数 | 片段数（chunks） |
|---|---|---|
| 白酒 | 28 | 2,973 |
| 红利 | 28 | 4,972 |
| 贵金属 | 30 | 3,601 |
| 宏观 | 64 | 1,048 |
| **合计** | **152** | **12,594** |

检索时：把用户问题也变成向量 → 在对应行业集合里算余弦相似度 → 取最像的 5 段 → 拼进提示词让大模型基于它们作答。**页面上能看到每一段召回的原文、相似度分数、出自哪份文件哪一页。**

### 5.3 重排（Rerank）：让"最正确的那一段"排到前面

我们实测发现一个现象：**"翻到对的公司"很容易（96.3%），但"翻到对的那一整段"很难（7.6%）**。因为同一家公司上百个段落长得太像（都是"营业收入同比增长 X%"这种句式），向量检索分不清。

于是做了**两段式检索**：bge 先粗召回 Top-100 候选 → 再用**混合重排**（0.6×bi-encoder 余弦 + 0.4×late-interaction MaxSim）精排取 Top-5。

> 效果：全局精确命中率 7.27% → 7.59%（+0.32pt；白酒/红利上升，**贵金属小幅回落** 6.08%→5.82%）。提升不大，但它暴露了真正的天花板：**约 63% 的正确段落根本不在 Top-100 里**——重排救不了没被召回的东西。真正的杠杆是换更强的向量模型 / 查询改写 / 更细粒度切分。

### 5.3.1 一次"回头质疑自己"的实验：我选的重排器其实不是最优的

做完上面那轮，我复盘时发现一个问题：**我当初为什么没用最强的重排方法（cross-encoder）？** 写在备注里的理由是"模型下载太慢"——**那是环境不允许，不是技术上它更好**。但我后来却把它当结论写进了文档。

于是我把那个模型改用 ONNX 方式加载（原格式用常规方式读不了），做了**三路受控对照**——同一批问题、同一批候选、同一套判分，只换最后排那一下的人：

| 排序器 | 精确命中率（n=100） |
|---|---|
| 纯向量基线（不重排） | 6% |
| late-interaction（当时上线方案） | 10% |
| **真 cross-encoder** | **17%** |

**四个行业上顺序完全一致。** 所以我把文档里那句"late-interaction 是更实际的选择"改成了：**"它是 cross-encoder 不可用时的退路。"**

⚠️ 必须说明：样本只有 100 条（每行业 25 条），**绝对幅度不能当结论用**，要升级为主指标需全量重跑 2,754 条。**但方向是稳的。**

两件事并不矛盾：**候选池内部还有空间（换重排器就能拿到），池外那 63% 谁都救不了（必须提覆盖率）。**

### 5.4 模型与微调

- **推理侧**：DeepSeek（真实模式）为主；另外内置讯飞星火 Spark 4.0 系列的模拟链路（可调 temperature / top_k / max_tokens，支持流式与模拟报文）。
- **微调侧**：提供 SparkPro + LoRA 方案（lora_r=16、alpha=32、lr=8e-5、epochs=5），在 FinCUGE / DISC-Fin-SFT / FinEval 上训练评测。这部分是"可落地"的工程资产，不只是演示。

---

## 六、评测：我们怎么证明它有效？（这节的诚实度是项目亮点）

### 6.1 检索评测：2,754 条查询实跑

**测试集怎么来的（这是关键设计）**：不是人工瞎写几个问题（那样指标会虚高、还容易作弊），而是用**业界标准的 chunk-derived 方法**——从知识库真实的 12,594 个片段里挑出合格片段，**反过来为每个片段生成"答案就在这一段里"的问题**，这个片段所属的公司/政策就是标准答案（qrels）。思路与 BEIR / RAGAS 同源，无需人工标注，也没法作弊。

**结果（2,754 条查询，bge 实跑，混合重排后）**：

| 指标 | 数值 | 人话解释 |
|---|---|---|
| Recall@5 | **0.9628** | 96.3% 的问题，前 5 条里能翻到正确公司的资料 |
| Precision@5 | 0.8881 | 前 5 条里 91.6% 是有用的（不是凑数） |
| MRR | 0.9385 | 正确资料平均排在第 1 位附近 |
| NDCG@5 | 0.9348 | 综合排序质量 0.94（考虑"排第 1 比排第 5 好"） |
| **精确 chunk 命中率@5** | **0.0759** | ⚠️ 只有 7.6% 能精确定位到"源头的那一段"——诚实公开的短板 |
| 来源覆盖（Top-5） | 2.521 | 前 5 条平均来自 1.74 个不同文件 |
| 余弦相似度均值 | 0.6651 | 召回片段与问题的相似度 |

**分行业看（差异很有信息量）**：

| 行业 | 查询数 | Recall@5 | 精确命中@5 |
|---|---|---|---|
| 白酒 | 200 | 1.000 | 0.0742 |
| 红利 | 290 | 0.9931 | 0.0587 |
| 贵金属 | 239 | 1.000 | 0.0582 |
| **宏观** | 153 | **0.8562** | 0.1123 |

> 宏观最弱（Recall 只有 86.6%）——因为政策文件措辞抽象、主题分散，是下一轮优化重点。红利精确命中最低（5.9%）——因为分红政策段落彼此几乎一模一样。

**为什么精确命中率这么低？我们做了归因**：
- 失效模式 A（约 12%）：**亲兄弟挤占**——正确段落排在第 6–20 名，被同公司的其他段落顶掉。
- 失效模式 B（约 63%）：**淹没在同公司段落海**——正确段落整库排到 100 名开外。
- 佐证：未命中的查询里，**86.6% 的 Top-5 至少包含 1 个同公司段落**；Top-5 中平均 2.95/5 条来自同一家公司。说明**实体级检索极准，只是"具体哪一段"定不准**。
- 宏观反而是反例佐证：它文档多但每篇短、同质度低，精确命中率反而最高（11.2%）。

> **"低"不等于"坏"**：对 LLM 作答来说，同公司的另一段有用信息通常同样有效（生成指标 0.89 就是证据）。这个指标更像**"排序特异性"报警器**，它精准指向了下一步该做什么（重排 / 按公司分桶 / 难负样本微调）。

### 6.2 生成评测：答案到底有没有瞎编？

只验证"翻对资料"还不够，还得验证"答案是不是真的基于资料写的"。所以做了**生成端评测**（RAGAS 的另一半）：

- 用**与线上专家智能体完全相同的 system/user prompt**，把 Top-5 片段拼进去生成答案；
- 再让 **DeepSeek 当裁判（LLM-as-Judge）**逐条打分（0~1）。

**结果（deepseek-chat 实跑 2,754 条，`mode=llm` 真值）**：

| 指标 | 数值 | 人话解释 |
|---|---|---|
| **Faithfulness 忠实度** | **0.8988** | 90% 的内容能在检索到的资料里找到依据，**基本没有幻觉** |
| **Answer Relevance 答案相关性** | **0.8666** | 87% 的回答是切题的，没有答非所问 |
| **Context Utilization 上下文利用率** | **0.8744** | 89% 的回答真的用上了检索片段，不是放着不用 |

分行业忠实度：白酒 0.909 / 红利 0.911 / 贵金属 0.934 / **宏观 0.841**（宏观再次垫底，与检索短板一致）。

> 没有 API Key 或断网时，脚本会**自动降级为离线代理指标**，并且如实标注 `mode=proxy`——不偷偷用假数字冒充真值。这份"诚实"本身就是工程素养。

### 6.3 其他评测

- **AI as Judge**：四组方案打分对比（26.41 / 25.06 / 26.47 / **28.41**），配显著性检验（p=0.017）。
- **AI as Customers**：让 AI 扮演不同类型的客户提问，测系统的鲁棒性。
- **人工评估 + 消融实验**：拆掉某个模块看指标掉多少，验证每个环节真的有用。

---

## 七、工程与部署（踩过的坑都在这）

- **部署形态**：单文件 Streamlit 应用，部署在 **Streamlit Community Cloud**，永久公网可访问，改代码推 GitHub 即触发更新。
- **鲁棒性设计**：
  - RAG 检索用**通用递归收集器 + 多级兜底 + 防御式类型转换**，彻底消灭"本地正常、云端返回空"这类玄学问题（这是真实踩过的：云端拿不到检索结果，根因是集合结构可能是 list 而非 dict、query key 可能不是字符串）。
  - 应用内**自诊断面板** + **构建指纹**（页脚显示 git 短哈希），一眼看出线上跑的是不是最新版。
- **安全合规**：密钥不进代码库（`.streamlit/secrets.toml` 已 gitignore）；用户 Key 只存在浏览器会话；所有个股分析都附风险提示并声明"不构成投资建议"。
- **访问闸门**：演示/真实模式有门禁控制，防止公网误用 API 额度。

---

## 八、关键数字速查（讲的时候别背错）

| 项 | 数值 |
|---|---|
| 知识库 | 4 个行业集合 / **152 份文档 / 12,594 个片段** |
| 向量模型 | BAAI/bge-small-zh-v1.5 |
| 检索形态 | 两段式：bge 召回 Top-100 → 重排 → Top-5（**当前重排后端为次优的 late-interaction**） |
| 重排三路对照（n=100） | 纯向量 **6%** / late-interaction **10%** / **真 cross-encoder 17%**（四行业排序一致） |
| 评测查询数 | **2,754 条**（chunk-derived，白酒755/红利852/贵金属773/宏观374） |
| Recall@5 / Precision@5 | **0.9628 / 0.8881** |
| MRR / NDCG@5 | 0.9385 / 0.9348 |
| 精确 chunk 命中率@5 | **0.0759**（重排前 0.0727，短板，诚实公开） |
| 生成三指标（LLM-as-Judge） | 忠实度 **0.8988** / 相关性 0.8666 / 利用率 0.8744 |
| 咨询流水线 | 7 个节点（leader→retrieve→expert→critic⇄revise→verify→summarize） |
| 荐股筛选树 | 12 个节点，5 支候选 → Top-3 |
| 基础过滤 | 市值 > 500 亿、非 ST |
| 微调配置 | LoRA r=16 / α=32 / lr=8e-5 / 5 epoch |
| AI as Judge 最佳均分 | 28.41（p=0.017） |

---

## 九、常见疑问（FAQ）

- **Q：和直接问 ChatGPT 有什么区别？**
  A：多智能体流水线 + 知识库取证 + 自我批评回环。**过程可见、信源可溯、幻觉可控**，而不是单轮黑箱。

- **Q：数据是真的吗？**
  A：知识库来自**真实年报/季报/货币政策报告**（152 份）；但个股评分与推荐是**模拟演示**，页面已明确声明不构成投资建议。

- **Q：评测指标为什么不是 100%？**
  A：**故意的**。指标一旦满分，通常说明测试集太简单或评测设计有问题。我们混入难例、公开短板（精确命中 7.6%、宏观 Recall 86.6%），这样的数字才敢拿去讲。

- **Q：精确命中率这么低，系统是不是不好用？**
  A：不影响使用。实体级召回 96.3%、生成忠实度 0.90——LLM 拿到同公司的另一段有用信息，同样能答对。精确命中率是**排序特异性诊断指标**，用来指路优化，不是可用性指标。

- **Q：能接自己的模型吗？**
  A：可以。推理侧支持自带 DeepSeek Key 或星火；知识库的 Embedding 接口也可替换。

- **Q：这个项目最难的部分是什么？**
  A：不是"接个大模型"，而是**三件事**：① 让检索在云端和本地行为一致（防御式工程）；② 设计一套**不可作弊、与业界对齐**的评测；③ 有勇气把难看的指标（7.6%）也写进产品页。

- **Q：你最得意的一次技术判断是什么？**
  A：不是某个优化，而是**一次自我推翻**。我上线用的重排方法，当初选它的理由是"模型下载不下来"——那是环境约束，我却把它写成了技术结论。后来我把那个模型改用 ONNX 加载补做对照，发现它比真 cross-encoder 差一档（10% vs 17%）。**能把自己的技术选型当成假设去验证，比做出一个更好的方案更难。**

- **Q：那你的重排到底有没有用？**
  A：要分两层说。**候选池内部有用**——换更强的重排器还能明显提升（6%→17%）；**候选池外部没用**——63% 的正确段落压根没被召回，再强的重排也够不到。所以真正的主线任务是提高召回覆盖率，重排只是我们已有的一个次优环节。

---

## 十、局限与下一步（诚实收尾）

**已知局限**
1. **精确段落定位弱**（7.6%）：同公司段落高度同质，且约 63% 的正确段落落在 Top-100 之外。
2. **宏观行业最弱**（Recall@5 86.6% / 忠实度 0.841）：政策文本抽象、主题分散。
3. **演示数据限制**：荐股评分基于模拟数据，非真实行情。
4. **重排后端是已知次优的**：上线用的是 late-interaction（因为 cross-encoder 模型当时加载不了）；三路对照显示真 cross-encoder 更强（n=100：17% vs 10%），但该结论尚未全量重跑确认，因此**目前页面上的精确命中率仍基于次优后端**。

**下一步方向（按性价比排序）**
1. **把重排后端换成真 cross-encoder 并全量重跑**（最便宜、最确定的一步：已完成 ONNX 推理路径与三路对照，只差把样本从 100 条扩到 2,754 条）。
2. **换更强/领域化的向量模型**（bge-large 或金融域模型）+ 重嵌知识库 → 直接抬高召回覆盖率天花板。
2. **查询改写 / 扩展**（让模糊问题的正确段落进得了 Top-100）。
3. **更细粒度切分 + 按公司分桶**，减少同质"兄弟段落"挤占。
4. **难负样本微调**：专门用同公司相似段落做负例训练。
5. **补齐宏观语料**（政策解读、券商宏观研报），针对性补短板。

---

## 十一、收尾：这个项目真正的价值

> Fin Synagent 的价值**不是"模型多强"**，而是把投顾分析这件事**过程工程化、透明化**——
> 多智能体分工、显式推理链、知识库取证、自我批评回环、诚实评测。
>
> 它既是一个能用的投顾助手，也是一套**可复用的 Multi-Agent 编排方法论**，
> 更是"如何诚实地评测一个 AI 系统"的完整示范。

---

*对应代码：`fin_synagent/app.py`（单文件应用）+ `kb.py`（知识库检索）+ `agent_graph.py`（咨询工作流）+ `screen_graph.py`（荐股筛选树）。评测脚本在仓库外 `build_kb/`（不进应用包，避免部署体积膨胀）。文中数字取自 `kb_data.json` 的 `retrieval_eval` 与 `generation_eval` 字段，随系统迭代同步更新。*
